# 更新日志

<!-- 每次发版在顶部补一条 "## 日期 主题" 段;后端 /api/version 取最新一条展示给用户。 -->

## 2026-09-01 概念生成先收敛再挖深;灵感工坊同款升级

**给写书的人——治「没灵感选个方向,出的概念还是不对味」**:

①**先挑故事内核,再出概念**:选完方向后,系统先用快档一次端给你 8 张一句话「故事内核卡」——每张标了主角类型×冲突来源×基调,互相拉开(不全是都市逆袭那套路)。挑 1-2 张(挑两张 = 让「这个的主角碰上那个的局面」)再深化成完整概念。换一批也不再是"换个模板",会把上一批顶掉重来。

②**偏好前移,不用裸猜**:选方向时先勾一下「想要的味道/元素、主角像谁、不想看到什么」(都可跳过),这些吃进生成——给系统的就不再只有一句「按都市套路来」。

③**灵感工坊对齐**:「AI 出方案」新增「没头绪?先挑几个故事内核」,走和上面同一条收敛→深化的路;自己已有想法仍可直出概念,不绕路。

**工程底座(用户无感)**:后端新增 `/api/inspire/engines`(快档出引擎卡,含换一批 avoid)/`develop`(深化/两张混搭)与 async 变体;方向屏四组轻偏好走 tendency 结构化注入;补 `projects.finished` 的 Alembic 迁移(修 CI 每次 schema 漂移门禁必红,存量库 `ADD COLUMN NOT NULL` 带 `server_default='0'` 不再炸);门禁全绿。

## 2026-08-31 写作区易用性四连 + 完本守护 + 去AI味闭环

**给写书的人**:

①**写作区好用了**:首次进来 3 步引导「选一章→让 AI 写→过目把关」;结果卡的 AI 味从分数改**轻/中/重**看结论,生成前先告诉你大概要多久,写完整章露出实际用时;检查明细(五维分/矛盾/守卫)收进折叠,干净章节不再被一堆技术行淹没;编辑态正文行宽收紧、两端对齐,长屏不累。

②**完本不再怕误删误改**:标了「完本」的书,改名/删除/清内容会被拦下,项目卡片挂绿色「完本」徽标。要改动需先取消完本。

③**治文风与字数双顽疾**:去 AI 味升级——规则扩容 + 双裁判 + 生成端自检 + 换代自愈,重 AI 味章节发布前自动回炉;大纲阶段就带上「总字数/每章字数」硬约束,不再出现"设三千字却写八千"。

**工程底座(用户无感)**:前端新增 `WriteGuide`/`genDuration` 成本预估;后端去 AI 味反馈闭环、完本接口 409 拦截与旧库迁移;新测试 520+ 用例;上述改动经门禁全绿。

## 2026-08-29 修投流误拦「先选命题」;全局输入框换新

**给写书的人**:①**投流生成就报错已修**——小说投流页签里情绪侧重默认就是「AI 按书自动挑」,但命题必填校验不分场景,选着默认项点「产 3 个本子」就被「先选命题」拦下;现在留空 = 放行让 AI 自己挑,独立工坊(情绪短片/灵感工坊)缺命题仍照旧要求先选。②**全局输入框美化**——全站约 67 处没写 type 的输入框此前是浏览器裸默认(矮、直角、深灰边,投流页的「氛围关键词」「一句话灵感」尤其难看),现在统一吃到全局样式;整套控件一起换新:高度 36→40px、圆角 10px、更轻的边框加一层轻内阴影、聚焦品牌色光环、下拉框换自绘细箭头(暗色自适应)、按钮与输入框同高同圆角不再错位,明暗两套主题都校过。

**工程底座(用户无感)**:

* 后端 `_validate_common` 增加 `derived` 参数,小说衍生(source\_project\_id 非空)放行空命题;新增回归测试

* 前端表单控件选择器改 `input:where(:not(…))` 兜住无 type 裸框,`:where()` 零优先级不翻组件覆写;新令牌 `--ctl-r`/`--ctl-inset`,select 去 appearance 自绘箭头,autofill 黄底压制

## 2026-08-29 新增「审校档」:写手与审校可以用不同模型了

**给写书的人——治「自己批改自己」的评分偏差**:此前草稿(写手)和主审/一致性门禁(审校)绑在同一个「快档」配置上,同一个模型自己写自己批,评分系统性偏差(恒 6 分)没法从配置上解开。现在模型设置新增第三个档位标记\*\*「审校」\*\*:主审评分、一致性门禁、定点修复全部走标了「审校」的配置——比如快档留 deepseek-v4-flash 写正文,审校标 deepseek-v4-pro,评分客观性和门禁准确度立上来。**不标审校档 = 跟随「默认」配置,行为与旧版完全一致。**

**工程底座(用户无感)**:

* 路由新增 `Tier.REVIEW`,`Task.CONSISTENCY`(主审/门禁/定点修复)迁到审校档;`resolve_tier_config("review")` 未设置时回落 quality 链

* `provider_configs` 加 `is_default_review` 列(alembic 0004,存量库自动补列),标记全用户唯一与默认/快档同机制

* 设置页模型卡新增「设为审校」按钮与「审校」徽标

* 新增测试:审校档解析回落、标记唯一性、路由绑定

## 2026-08-29 评分锚点化 + 回炉止损:治「每章烧满 3 轮还全部失败」

**给写书的人——回炉体系的两处系统性修复**:①**评分锚点化**:主审 prompt 此前只说「敢打低分,8 分以上必须名副其实」,没有任何 7 分是什么的定义,输出示例里的分数还被模型逐字照抄(线上 4 章 12 轮评分与示例一字不差)——现在每个维度给了明确的行为锚点,**7 分 = 合格发布线**(读者愿意付费追更),示例分数改为占位符,每维还要求一句话评分依据(编辑部评分卡悬浮可见)。②**回炉可收敛**:同一维度连续 2 轮评分无改善 → 不再为它重写;同一 blocker 连续 2 轮复现 → 止损隔离并提示「疑似检查误报」——不再每章陪跑满 3 轮。③**门禁误报三道闸**:证据剥引号归一(模型爱给证据加引号导致逐字匹配失败)、举证失败的 blocker/major 自动降为 minor(幻觉无一票否决资格)、时间自然推进未明说(午后→放学后)明确为 minor 衔接类,不算硬矛盾;单条 major 不再注定回炉(continuity 折算 7)。

**工程底座(用户无感)**:

* checker:证据引号归一 + severity 联动降级;`blocker_fingerprint`(描述指纹)供复现检测;timeline 判定细分写进 prompt

* 回炉循环:失败维度记账(逐轮对比,无改善即停滞退出)、blocker 复现止损隔离、逐轮 `rework_log` 落快照、`stall_note`/`gate_note`/`hints`(维度死锁提示)透传

* 回炉指令上限 500→800 字(此前多建议场景会把 blocker 修法截掉,writer 没看到要改什么)

* 测试更新/新增 12 项:continuity 新映射、单 major 通过、双 major 回炉、复现止损、维度停滞止损、证据引号归一/降级、评分依据解析

## 2026-08-29 任务能终止了 + 模型复读自动止损:不再陪跑烧 token

**给写书的人——生成不对劲时终于有刹车了**:①此前后台任务(生成/连写队列/润色/出片)一旦发起就**没有任何终止手段**,模型抽风时只能眼睁睁陪跑完甚至去换渠道「断粮」;现在**实时正文窗和任务中心都有了「终止」按钮**——点确认后立刻掐断进行中的模型调用,连写队列整条停止,半成品按既有失败语义回滚。②模型偶发**复读退化**(同一句话连抄几万字烧光输出预算):此前系统把它当「预算不足」翻倍重试、最多陪跑三轮,现在检测到截断正文里大量重复句子就**立即止损报错**,不再浪费后续 token;报错文案会提示直接重试或换模型。③修复「测试模型配置」对历史遗留的非法协议类型直接报 500 的问题,现在返回可读的错误提示。

**工程底座(用户无感)**:

* jobs 新增 job\_id → asyncio.Task 注册表与 `cancel_running_job`;`POST /api/jobs/{id}/cancel`(归属校验:非本人按不存在处理,已结束 400);CancelledError 由 fire\_and\_track 的 done 回调兜底落「已手动终止」,任务不再永挂 running

* `complete_text_with_budget` 截断分支加复读检测(切句统计唯一句占比,短文不判):复读即抛 UpstreamError 不再翻倍重试;正常截断仍维持 3 轮重试返回最长一次的语义

* 新增 8 项测试:复读检测纯函数/快败只调 1 次/正常截断不回归/spawn 与裸 fire 两种模式的终止/API 归属与状态护栏/坏协议测试兜 400

## 2026-08-29 问题面板支持「定点修复」:单条矛盾原位改句,不再逼你整章重写

**给写书的人——处理单条一致性问题有了第三条路**:此前问题清单里的一条小矛盾(称谓写错、时间表述反),要么「按建议修订」走整章重写(几分钟、好的部分跟着重掷),要么自己找句子手工改。现在每条带逐字证据的问题多了\*\*「定点修复」**按钮:AI 原位改句消掉这条矛盾(不重写),改完**重跑一致性门禁,复查干净才生效\*\*;复查仍有硬矛盾就一字不落、问题保持待处理,提示改走按建议修订。修复前正文自动存版本快照,可随时回滚。

**工程底座(用户无感)**:

* 新端点 `spot-repair`(异步 job,与按建议修订共享任务锁 409 语义):复用生成回炉的 `repair_chapter`/`apply_gate_fixes`(逐字+唯一锚校验),复查干净才落库

* 无逐字证据的问题不给定点修(无法定位),按钮不显示、后端 400 拦

* resolved 决议指纹锚定修复后正文,`persist_issues` 清账不再误清刚解决的记录

* 新增 4 项 API 测试:修复成功/复查 blocker 不落库/锚失配不动/无证据 400

## 2026-08-29 门禁分级回炉:小矛盾定点修,不再整章重写;门禁提到精修前

**给写书的人——审校不再「一刀切重写」**:此前一致性门禁只要发现矛盾,代价就是整章重写(草稿+定稿两遍大调用),写得好的部分跟着一起重掷。现在回炉改成**分级制**:①**门禁先行**——先确认事实对,再做文字精修,「精修了半天才发现方向错」从流程上不会再发生;②门禁发现 blocker 先**分诊**:称谓写错、时间表述反、道具没交代来路这类一句话能修的问题,走**定点修复**——一次小调用出「逐字锚 → 最小改动」的精确替换,锚必须全篇唯一(防误伤),改完重跑门禁复查,修不掉自动回退整章重写;③精修(校对+主审)只在门禁干净的文本上进行,主审触发的重写也先回门禁复查。整章重写只留给「要调整情节走向」的真结构性问题。

**工程底座(用户无感)**:

* 回炉循环重排:门禁(checker)提前到循环顶,校对/主审后置;`review_max_revisions` 预算两路共享,封顶 quarantine 语义不变

* checker 输出新增 `fix_mode`(patch|rewrite,缺失乐观判 patch——判错只损失一次小调用);`triage_issues` 分诊(无逐字证据的问题无法定位,整批走重写)

* 新增 `repair_chapter`(定点修复调用,失败降级回退重写)与 `apply_gate_fixes`(逐字+唯一锚校验替换,与校对自修同范式)

* 审校结果新增 `repair_rounds`/`repairs`(修复明细)字段,随生成结果透传;生成结果卡与主编评分卡回显「定点修复 N 处」徽标与改句明细

* 新增 4 项测试:定点修通过(不重写)/修不掉回退重写(只 patch 一次)/不可修直接重写/分诊与唯一锚纯函数

## 2026-08-29 审校回炉不再空转:门禁 blocker 收紧护栏,prose 回炉带具体禁则

**给写书的人——「每次都烧满 3 轮回炉」的根因找到了**:①一致性门禁的模型「宁高勿低」,把「手上伤口回现实没交代」这类细节标成 blocker(一票否决)——它自己的描述里都写着「尚可解释」;自认「属于新设定、不算冲突」的条目也标了 major。现在门禁提示词收紧 blocker 标准(三条同时满足才可标),并加了**确定性护栏**:描述里出现「尚可解释/建议在后续/无矛盾」这类打圆场措辞,自动降级,不再被一票否决拖进无谓回炉。②prose(文笔)维没达标时,重写指令此前只有一句「AI 腔」评语——同一个模型自由发挥修不掉;现在回炉指令自动附带**具体禁则**(段落开笔方式、情绪外化、比喻与套话限制、对话与句式要求),回炉才有实际效果。

**工程底座(用户无感)**:

* 门禁提示词 blocker 判定改为三条件同时满足,「拿不准就降级」写进提示词

* `_normalize_issue` 增加 severity 降级护栏(打圆场措辞 → 降级),宁可低估

* 新增 4 项测试:打圆场 blocker 降级、自认非冲突降 minor、真硬矛盾不误伤、prose 指令按分追加

## 2026-08-29 蓝图 0 章解析修复:容忍 markdown 章节头,格式崩坏重试带格式补丁

**给写书的人——「0/11 章」那种失败根治了**:有模型会把章节头写成 markdown 标题(`# 第1章 - 血月之邀`),此前解析器只认「**第1章」加粗前缀,整块蓝图一个章都解析不出(0/N),三次续补全部报废。现在:①解析器容忍** **`#`/`##`/`###`** **标题前缀与全角冒号分隔;②一章都解析不出来时,续补的提示词自动附加**格式强提醒\*\*(阿拉伯数字章节头、逐行字段、禁 JSON),敲打模型回到正轨;③流式进度计数与解析器统一同一套匹配,进度不再骗人;④最终失败的报错会带上模型原文开头,问题一目了然。

**工程底座(用户无感)**:

* 蓝图格式约定在提示词源头明确「N 必须用阿拉伯数字」,把纠正提前到第一次生成

* 章节头匹配(流式进度/解析)统一收敛到 `blueprint_parser.count_chapter_heads`,不许两边漂移

* 新增 5 项测试:markdown 标题解析、全角冒号、朴素形式不回归、0 章续补带格式补丁、报错带原文

## 2026-08-29 分段整片提示词铺满全部工坊:漫剧/情绪短片也按 15s/30s 切段了

**给拍片的人——所有工坊的整片提示词现在都能直接用了**:此前只有宣传片按单段上限切段,这次把漫剧和情绪短片/灵感工坊/故事工坊也全部切成分段版——**漫剧集详情、clips 出片工作台**的整片提示词卡片新增「单段 ≤15s / ≤30s」选择器,生成的是「使用说明 + N 段各自独立可用的提示词」:每段单独贴进模型即可,按段号拼接成片。跨段风格一致靠每段逐字复述画风锚;漫剧涉及角色时逐字融入该角色外貌服饰(全片不漂移);台词/解说逐字嵌入并标注说话人;段间默认硬切,连续运镜跨段标注「末帧接首帧」。15s 短片一段装下,不显示选择器。

**工程底座(用户无感)**:

* 分段模板与文档头收进共享 `prompts/film_prompt.py`(五工坊一个模板,工坊差异只剩类型定位与原料块);漫剧/宣传片的旧单条整片模板删除

* 漫剧/clips 的生成端点同样接受 `segment_s`(白名单 15/30,越界 400),切分复用镜头边界贪心

* 新增 3 项测试:漫剧分段切分(15s/30s 两种上限)、非法段长拦截

## 2026-08-28 宣传片整片提示词改分段版:按 15s/30s 切好段,逐段生成再拼接

**给做宣传片的你——一条 60-90s 的片子不再没法用整片提示词**:外部视频模型单次最多生成 15s(少数 30s),一整条提示词直接贴根本用不了。现在宣传片「整片提示词」改为**分段版**:按镜头边界自动切好段(绝不在镜头中间断),生成一份「使用说明 + N 段各自独立可用的提示词」——每段单独复制贴进模型就能出风格一致的片段(段首逐字带画风锚),全部生成完按段号顺序拼接成片。卡片上可选**单段 ≤15s / ≤30s** 适配你的模型;段间默认硬切,连续运镜跨段的罕见边界会标注「末帧接首帧(图生视频)」。

**工程底座(用户无感)**:

* 段切分复用内部出片链的镜头边界贪心 `group_by_limit`,单段上限 `segment_s` 白名单 15/30(越界 400)

* 文档头(使用说明/段数/拼接指引)由引擎确定性生成,模型只写各段提示词块,段内解说词逐字嵌入并反查说话人

* 共享卡片组件增加 `headerExtra` 插槽(放段长选择器),其他工坊不受影响

* 新增 3 项测试:分段切分与段边界、30s 单段、非法段长拦截

## 2026-08-28 蓝图生成抗截断:欠章自动续补尾部,上下文超限报错说人话

**给写书的人——大部头/肥设定不再一碰蓝图就炸**:设定写得越肥,蓝图每章简述越长,一块 20 章越容易顶到模型单次输出上限被截断;此前截断后整块原样重摇 3 次——同样的输入得到同样的截断,必然再挂。现在改为**从最后一个已解析章之后自动续补尾部区间**直到补齐(续补的提示词只请求缺失的那几章),连续续补仍不达标才明确报错并说明出路。另把「输入+输出超出模型上下文窗口」的上游英文报错统一翻译成可操作的中文:精简设定、减少一次生成的章数,或换上下文更大的模型。

**工程底座(用户无感)**:

* `generate_blueprint` 分块循环重写:欠章按 `已解析至+1` 续补(CHUNKED 模板按区间请求),段间同章去重(以后者为准),缺章警告只在块级算一次,不再刷中间态

* `normalize_job_error` 新增上下文超限识别(context length / too many tokens 等关键词命中即翻译)

* 新增 4 项测试:分块截断续补、整书模式(≤20 章)截断续补、持续欠章明确报错、超限文案归一化

## 2026-08-28 整片提示词铺满全部工坊:宣传片/情绪短片/灵感工坊/故事工坊都能一键复制出一整片

**给拍片的人——不再只有漫剧能整片出**:上一版只有漫剧工坊有「整片提示词」,这版把它铺到了其余所有工坊——**宣传片**在工作台交付区、**情绪短片/灵感工坊/故事工坊**在出片工作台顶部,都能「AI 生成整片提示词」并一键复制,贴去 Sora / Veo / 可灵一次出一整片;同样支持手改与整段粘贴自己的版本。各工坊原料不同:宣传片带地标卡与素材点(卖点硬约束),情绪系带命题/点子与金句收束,故事工坊照你的原创点子拍。

**工程底座(用户无感)**:

* 五段式手艺收进共享模板 `prompts/film_prompt.py`(总述/一致性/时间码镜头表/声音设计/影像限制),工坊差异只剩类型定位、原料块、声音规则三处;漫剧侧同步复用共享的说话人反查与剥围栏小件

* `mood_clips` / `promo_plans` 补 `film_prompt` 列(legacy 迁移幂等 + Alembic 0003);三端点(POST 生成/GET 读取/PUT 替换)按各工坊既有归属与 job 去重惯例接

* 前端卡片收敛为共享组件 `ui/FilmPromptCard`,漫剧侧改为复用,一处改处处生效

* 新增 6 项测试:两种原料形态(clips 行内 JSON / promo 独立表)各验生成注入、无分镜拦截、整段替换、归属隔离

## 2026-08-28 整片提示词:分镜一键组装成「一次出一整片」的成片提示词,贴去 Sora/Veo/可灵

**给拍片的人——粘贴出片终于有整片的拿法了**:不想逐格出片?集详情新增「**整片提示词(端到端模型)**」——点一下,AI 把这一集的分镜、角色卡外貌服饰、画风、台词组装成**一条完整提示词**(总述定调 → 人物一致性档案 → 带时间码的镜头表 → 声音设计 → 影像限制),一键复制贴进 Sora / Veo / 可灵这类「一条提示词出一整片」的模型,一次生成整集。文本框可手改,也可以把自己写的版本**整段粘贴**保存;改了剧本/分镜点「重新生成」即同步。

**工程底座(用户无感)**:

* 生成骨架固化了整片提示词的手艺:人物一致性段逐字融入角色卡并强制「不面部漂移/不模仿真人」,镜头表按分镜时长累计时间码、台词原样嵌入并按剧本反查说话人,负面清单收拢画风负面词与口型错位等通病

* `drama_episodes` 补 `film_prompt` 列(迁移幂等);POST 生成(集级 job 去重)/ GET 读取 / PUT 整段替换三个端点;与三轨提示词互补定位,不动出片引擎

* 新增 4 项测试:无分镜拦截、生成注入原料+剥围栏、整段替换保存、归属隔离

## 2026-08-28 出片失败不再永挂「排队中」+ 下载体积上限

**给拍片的人——任务状态更诚实了**:出片在任何一步失败(首帧读取、配音、提交平台被拒等),任务历史里现在都如实标成「**失败**」并写明原因;此前 submit 之前的失败会让这一格永远停在「排队中」,像还挂着个假任务。另外下载加了 **500 MB 体积上限**:平台地址异常返回超大文件时立刻中止并说明,不再无声耗着。

**工程底座(用户无感)**:

* 失败标记收口:`start_render` 统一兜底,任何异常先 `_mark_failed` 再上抛,内层散落的标记调用全部移除(单一出处,以后新增失败路径不会再漏)

* `fetch_bytes` 改流式下载:Content-Length 声明超限一个字节都不读;没声明长度的边收边数,收满上限即断连接,内存占用封顶

## 2026-08-28 出片下载链路 SSRF 加固:外链与平台回传地址不许指向内网

**给拍片的人——异常提示更准了**:出片时如果平台返回的成片/配音地址、或你挂的外链首帧指向了内网/本机,不再报一句莫名的「下载失败」,而是直接说「**地址不安全,不能指向内网/本机**」并给出下一步(外链首帧请下载后重新上传;平台地址请重试一次),任务如实标失败,不再挂着「进行中」。

**工程底座(用户无感)**:

* **下载器自带出站校验**:`fetch_bytes` 现在对**每一跳**地址做私网/环回/云元数据(169.254.169.254)判断。此前只在调用点查入口 URL,而 httpx 会自动跟随重定向——「给你一个公网地址、302 跳进内网」这条经典绕过是通的,现在逐跳校验堵上,并设了重定向次数上限,防止被遛着转圈烧掉下载超时预算

* 两道防线并行:保存配置时校验 `base_url`(原有)+ 下载时校验文件地址(新增),任一命中即拦。DNS rebinding 仍在这两道之外,已在 `net_guard.py` 记账

## 2026-08-28 完整档第三步·一键合成整集:拼接 + BGM 垫底 + 字幕,一条成片

**给拍漫剧的你——「一键成片」落地了**:完整档下,集详情最下面新增「**合成成片**」——传一首 BGM、决定要不要字幕,点「合成整集」,ffmpeg 在本机把逐镜草片按镜号拼成一条整集 mp4,就地预览/下载:

* **缺格不断片**:没出视频的格用**静帧定格**占位(该格时长照旧),连静帧都没有的格跳过并在结果里如实说明——出片到一半也能先合成看整体节奏

* **音轨不打架**:对白格的配音随片内嵌保留;你上传的 BGM(每集一段,MP3/WAV ≤15MB,重传即换可试听)自动**低音量垫底**,人声与垫乐分层

* **字幕可选烧录**:按每格台词与**实际片段时长**(ffprobe 实测)压中文字幕,不想要关掉即可

* 合成前后看得见:按钮上就有「可拼 N · 静帧占位 M · 跳过 K」统计;同集只留最新一条成片(旧的自动清);成片不参与「设为成片」,直接预览/下载

* 依旧只在**完整档**下出现(项目设置切换);ffmpeg 缺失时按钮禁用并给安装指引

## 2026-08-28 完整档第二步·末帧自动接力:上一镜的结尾,就是下一镜的开头

**给拍漫剧的你——镜头衔接不用手工记了**:分镜出片成功后自动用 ffmpeg 抽出该片**最后一帧**存档;下一格的挂静帧区会出现「**用上一镜末帧当首帧**」按钮(带缩略图),点一下就把上镜结尾挂成本格首帧——本格出片即从上镜结尾接着动,镜头自然连贯。这就是「上镜末帧=下镜首帧」衔接协议的机械实现:

* **一键采纳**:末帧直接由服务端挂为本格静帧(自动勾「静帧出好了」),段计划的「首帧图已就位」立刻亮;重 roll 后末帧跟着更新,永远取最新一版

* **能力自检**:部署里有 ffmpeg 才显示候选(桌面安装包已支持随包携带;Docker 镜像已预装;源码环境 PATH 里有即可,或设环境变量 `JARVIS_FFMPEG` 指到可执行文件)——没有它按钮不出现,其余功能零影响

* ffmpeg 二进制**不进仓库**(体积/许可):桌面构建前手工放 `backend/bin/ffmpeg.exe`(构建脚本会提示),Docker 与源码环境见上

## 2026-08-28 完整档第一步·对白配音链:对白分镜格出片 = 自动配音 + 对口型

**给拍漫剧的你——角色开口说话了**:项目切到「完整档」后,有台词的格点「本站直接出片」不再只是画面动起来,而是全自动「**台词 → indextts2 按角色音色配音 → H3 对口型**」,出一条开口说话的成品镜:

* **角色音色参考**:每张角色卡新增「音色参考」——传 5-10 秒干净人声(MP3/WAV ≤8MB,重传即换,可试听),对白出片时按它克隆该角色嗓音;没传音色的对白格回退普通出片,任务说明里写明原因,不静默出错

* **配音情绪**:分镜格上选(平静/开心/愤怒/悲伤/惊恐/厌恶/惊讶/忧郁,点一下就存),喂给 indextts2 的情感权重;默认平静

* **真时长驱动画面**:配音的真实音频时长(wav 头解析,不装 ffmpeg)决定成片的 `audio_duration`——先配音后定长,TTS-first;配音超过工作流 15 秒上限时照常出片但任务里如实标注「已截断,建议拆格」

* **配音缓存**:同一「台词+音色+情绪」重 roll 视频不重付配音钱,时长也不漂

* **说话人自动匹配**:哪句台词是谁说的,按剧本台词反推(与配音稿同一套匹配口径),再按角色名/别名找音色

* 设置 → 出片引擎新增配音/对口型两个工作流 ID(留空用默认 indextts2-v1 / minimax\_h3\_image\_audio\_to\_video);对白出片两步只在完整档生效,轻量档行为零变化

## 2026-08-28 出片引擎(轻量档):漫剧/情绪短片镜头上直接点按钮出视频草片

**给拍片的人——不用出站了**:之前出视频要逐格复制提示词、去视频站粘贴、等结果、下载、回站里记进度;现在漫剧分镜格与情绪短片出片工作台上点「**出片**」就能直接出视频草片,出完就地预览:

* **一键出片 / 重 roll**:有静帧的格走**首尾帧**工作流(静帧当首帧,人物长相稳),没静帧走**文生视频**;提示词由引擎按「只写运动、不写外貌」的口径自动拼好(与「一键粘贴」同一套规则,不会两路口径)。出的草片就地预览,不满意**重出一版**(几分钱一次),版本都在列表里,挑中意的点「设为成片」——「视频出好了」的勾仍由你自己打

* **接入方式**:autodl.art 托管的 ComfyUI 工作流(MiniMax H3),到「设置 → 出片引擎」填一次令牌即可(令牌加密存库、界面打码);计费约 ¥0.02/秒、凌晨 ¥0.01,画质档 480p/768p 可选,竖横按各片画幅自动折算。工作流 ID 可改,留空用默认

* **挂静帧更放心**:上传静帧/参考图时自动比对画幅,横竖搞反会提醒一句(不拦,你拍板)

* **两条路都通**:点按钮出片,或照旧「一键粘贴」去即梦/可灵——不配令牌时出片按钮处有明确指引,老流程一点没变

**引导**:漫剧工坊引导卡新增「分镜格里直接出片」三步说明;步骤条新增「**逐格出片**」步(带 已出 N/M 格 的实时进度);未配令牌时空态直接给「去设置」的路。项目设置新增「**出片模式**」开关:轻量档(现在这个)/ 完整档(对白配音链、首尾帧自动接力、一键合成——分期上线,切换不丢任何数据)。

**工程底座(用户无感)**:出片任务/配置两张新表与 `render_mode` 列入基线迁移(schema 漂移门禁同步更新);视频生成共用件(运镜词表/负面词/时长与分辨率口径)下沉 `media/video.py` 供漫剧与情绪两线共用;删项目/删短片时渲染任务与草片文件级联清理;架构决策见 `docs/adr/0003`。

## 2026-08-27 镜头拆细到 1 秒一格 + 四条出片线「一键复制全部提示词」+ 桌面启动迁移加固

**给拍片的人——镜头拆细,单格更稳**:视频模型单格渲染质量差,之前 3 秒一格容易糊成一片。现在漫剧分镜按 **1 秒一格** 拆(镜头上限随目标时长涨到 \[8,120] 格、单格最长 3 秒),让每格只承载一个看得清的小动作,成片拼接反而更稳:

* **漫剧分镜细拆**:动作/空镜/氛围格 1 秒、对白格按台词长度 1-2 秒;提示词要求每一格写清「这个动作从哪里起、到哪里止」,景别/运镜靠变化补足 1 秒画面的节奏

* **四条出片线统一「一键复制全部提示词」**:不再一格一格复制——漫剧三轨提示词的「复制全部中文」、宣传片切段的「复制全部段提示词」(5/10/15 秒段整片按段号首尾相接)、情绪短片与生日短片的「复制全部镜头中文」。整份按镜头号/段号顺序、空行隔开、只带中文主链路,粘到文档/交模型/批量出图都顺手

**桌面版工程(用户无感,但让启动不再翻车)**:

* **Alembic 迁移路径加固**:桌面安装包曾有几率漏打包 alembic 配置,升级老库时迁移静默失败 → 新列建不出来 → 首页 500、卡在启动页。现在迁移脚本目录**程序化注入**(不再赌 ini 配置是否完整打包),漏包时显式告警而不是无声退到兜底路径,升级老版桌面后的首次启动更稳

## 2026-08-27 漫剧出片新增「参考生视频·多图主体绑定」+ 数据库迁移体系落地 + 项目整本导出/导入

**给拍漫剧的你——第四种出片拿法(r2v·参考生视频)**:角色定妆照按提示顺序上传当参考图,人物身份由参考图像素级锁定、场景交给模型发挥,**跳过逐格出静帧**的步骤。Vidu / PixStag / 可灵多图这类支持多图参考的站点直接可用:

* **【主体绑定】块自动生成**:每格分镜/每个视频段的粘贴版新增「参考生视频」平台——`参考图1 = 「角色A」…参考图2 = 「角色B」`,hint 写明第几张是谁、文字与参考图冲突时以图为准;只有真有定妆照的角色才进绑定块,还没有的明确指路"先回角色卡出定妆照",不让人空跑

* **段级主体自动并集**:几格并成一段时,主体按段内各格出场去重连续编号;拍摄手册(Markdown)新增第④节、逐格施工单 CSV 新增 `paste_r2v` 列,三条出口同一套料

* 音频分轨口径(环境音留给模型 / 人声走配音稿一字不差 / BGM 整片统一铺)经同行工作流交叉校验,维持不变

**工程底座(用户无感,但从这版起走得稳)**:

* **数据库迁移正式接入 Alembic**:桌面版升级后首次启动会把现有数据库**自动登记到迁移基线——只写版本号、不动任何数据**(全新安装由基线迁移直接建全量表);此后的表结构变更全部走版本化迁移,老用户升级不再有改表风险。全新库/存量库两条路径各有自动化测试钉住,CI 另加 **schema 漂移门禁**(改了模型漏生成迁移时直接红)

* **日志带上追踪上下文**:每条日志标注请求 ID / 用户 / 任务号,响应头带 X-Request-ID——排查问题按号过滤,不再大海捞针

* **项目整本导出/导入**:一本书打包成 JSON(架构/大纲版本/章节版本/故事圣经/伏笔/写作卡/倾向预设),备份、搬迁、双机同步有了正规通道;归属校验与格式版本号齐备

* **CI 正式建立**:每次 push/PR 自动跑后端全量测试 + 前端 lint/typecheck/build/vitest

* 内部整理:漫剧千行接口文件按资源拆包(33 条路由逐一比对零变化)、五个模块的提示词外置为模板文件(调词不再动代码)

## 2026-08-27 新增故事工坊:我突然有个点子,直接拍 + Q版沙雕漫画画风,顺手修两个实测撞上的 bug

「自己冒出一个具体场景」终于有了自己的入口。此前情绪/灵感工坊都是「选命题让 AI 编」——你想拍的「小朋友和妈妈坐出租车,车上一段搞笑对话」只能塞进 40 字自定义框,生成的还是 AI 的三个变体而不是你的故事。这条线把心智掰正:

* **故事工坊(`/free`,第五个工坊)**:一段话把点子写下来(上限 500 字),**点子即命题**——AI 以你的点子为本子主轴:第 1 个本子忠实还原你的故事(人物/场景/对话与笑点照它原样出),另两条保留同一场景换镜头语言/叙事顺序的变体,提示词层明令「禁止另起炉灶编新故事」;带意见「换一批」「单条重拍」照常可用,且都围着你的点子收敛,不再三选一里没有一个是你想要的。

* **Q版沙雕漫画画风档**:二头身大头小身、粗黑描边、平涂上色、表情包式夸张表情(瞪圆眼/冒汗/张嘴大喊),条漫/表情包插画质感——搞笑日常与段子类的主力画风。画风目录三条出片线(漫剧/宣传片/短片)共用一份,加一档全线生效。

* **修:工坊一生成就弹回小说列表**。灵感/情绪工坊点「产 3 个本子」后直接跳进「我的小说」、工坊完全无法操作——路径拼出 `//inspire/42` 这类双斜杠,命中不了任何路由、被兜底重定向回首页。现在创建、点入已有条目、「← 返回」全部落在正确路径。

* **修:场景节拍上线前的老书,大纲页直接 500**。旧数据的节拍/人物/道具 JSON 列为 NULL 时,响应模型的字段默认值不兜底、序列化即崩——任何带老数据的库都踩得到,已修,老数据无需迁移。

* **官网与 README 截图全面翻新**:桌面四图 + 移动端六图全部实拍当前 UI(工作台/首页/应用锁/故事工坊/导航抽屉/正文/阅读器/全书概览),删掉过期的级联演示 GIF;README 与官网补齐了此前完全缺失的灵感工坊、故事工坊介绍。

## 2026-08-26 工坊修「情绪任务凭空消失」:独立工坊不再混入小说投流企划,返回跟进入路径走

一组实测复现的工坊顽疾:「在情绪短片/灵感工坊选好内容生成,结果跳进了小说,情绪任务找不到了」。链条是两个缺陷叠加——

* **独立工坊列表混入小说衍生条目**:后端列表接口在工坊场景只按类型过滤、不看数据归属,你在小说里建过的投流短视频企划全部混进工坊列表(还按最近更新排在最顶上),界面零区分。现在独立工坊只显示工坊自建的条目,小说衍生的留在小说「投流」页签,各归各位。

* **工作台返回按数据归属跳转而非进入路径**:返回按钮只要发现企划挂着小说来源,就无条件把你送进小说书页——不管你从哪进来的。生成完唯一出口一按,工坊上下文凭空消失。现在列表进入工作台时带上「返回到哪」,从哪个列表进来就回哪个列表;刷新/直链丢状态时才按数据归属兜底。

* 普查了其余工坊:宣传片(`nav("/promo")`)、生日祝福(`nav("/birthday")`)的返回本就固定回自己列表、无小说数据关联,漫剧是小说原生页签——均无此问题,不需改动。

* 回归测试钉住「独立列表不含小说衍生条目、小说页签过滤不受影响」这一格。

## 2026-08-26 桌面版启动体验大修:打开秒响应(splash)、连点不再拖出多实例、端口占用从根上消灭

针对一组用户实测撞上的启动问题做的一次彻查与修复:「装完打开一次没反应 → 一直点打开 → 端口被占用 → 什么都打不开」这条连锁故障链,拆开是四个环环相扣的坑:

* **打开秒响应(治「没反应」的根)**:旧启动顺序是「拉起后端 → 等它就绪 → 才建窗口」,首次启动/更新后首次启动要过杀毒软件全量扫描(PyInstaller 解包 + 数百个依赖文件),几十秒内桌面毫无动静,用户自然以为没打开而连点。现在**窗口瞬间出现**,先显示本地 splash 页(转圈 +「正在启动本地服务…」+ 首启慢是正常现象的提示),后端在后台线程拉起,就绪后窗口自动跳转到工作台。等待期间关窗也会正确杀掉后端,不留孤儿进程。

* **连点不再多实例**:单实例插件(tauri-plugin-single-instance)在 Windows 上有个竞态洞——它靠「找到首实例的隐藏窗口」来拦第二实例,首实例还在初始化时窗口没建好,第二实例就被**静默放行**,照样拉起一份后端抢端口/锁数据库。现在在 Tauri 初始化之前先做一道**内核级互斥体守卫**:健康首实例在世时把已有窗口唤到前台,启动竞态窗内绝不放行第二份后端;更新重启(新进程先起、旧进程后退)也能正确接管不误杀。守卫语义经双进程实测三场景验证,并配有单元测试钉住。

* **端口占用从根上消灭**:后端旧逻辑是「探测 8756 空闲 → 释放 → 重导入 app(数秒)→ uvicorn 再绑定」,探测与真正绑定之间存在数秒窗口,多个实例或他进程可在窗口里抢走端口,uvicorn 绑定失败即退出。现在改为**先重导入、再一次绑定并把 socket 原样交给 uvicorn**(`serve(sockets=...)`):8756 空闲就用 8756,被占则由操作系统**原子**分配空闲口,打印的地址与真正监听的地址永远一致,竞态窗口不复存在。

* **报错说人话**:旧超时弹窗把锅甩给「端口被占用或数据目录被锁」——端口现在不可能被占死了,真正的元凶多半是杀毒扫描。超时上限 30→60 秒(splash 有反馈,等得起),文案改为如实告知最常见原因与加白名单的办法。

## 2026-08-25 新增生日祝福工坊:寿星定制祝福视频(第四条出片线)

给「不写小说也能用这台制片台」再开一条线(`/birthday`):填一份寿星资料,AI 一次产 3 个不同切入的定制本子——开场点名抛悬念、回忆杀落在你给的回忆点上、高潮收在吹蜡烛/拥抱那一帧。与情绪短片同构(批产三选一 → 手卡 → 出片盘),差异全在「定制」二字:

* **结构化寿星资料**:称呼(祝福台词点名)/ 关系 / 里程碑(周岁/成人礼/大寿快捷 chips)/ 回忆点 2-5 条(具体场景/梗/口头禅)/ 送出人;六大基调(整蛊爆笑/催泪走心/温馨日常/惊喜反转/燃向里程碑/宠溺可爱)各带**三幕节奏契约**——借鉴开源 hyperframes 工作流的「叙事模板+节奏契约」思路,结构铁律全部写进提示词正文。

* **回忆点防编造校验**(生日版的金句溯源):候选的分镜/台词逐条与回忆点做关键词核对,对不上的标进「需核实」——定制片最怕张冠李戴的「假回忆」,宁可误报让用户看一眼。手卡导出 md 带寿星资料卡+回忆点清单,出片时贴在旁边逐条核对。

* **分段出片玩法指引**(借鉴开源 ai-video-generation skill 的能力分类):普通段走文生视频;**回忆杀段传寿星真实照片走图生视频**(分镜提示词已含「保持参考照片面部特征与体型」,照片段写明这条脸才不漂);想让寿星照片开口说祝福,台词拿去对口型工具单独做;出片盘按段自动标注「图生视频 · 照片锚 / 文生视频」。BGM 风格建议按基调给(整蛊=滑稽拨弦/催泪=单钢琴…),只描述风格不点版权曲名。

* **儿童向风格包**(佩奇式/奥特曼式等六套内置「世界包」):每包自带强画风锚+世界观场景词+**主角植入指令**(寿星以该世界角色形象贯穿每一格——变身小小英雄与巨人并肩/当救援队长指挥狗狗队…)。版权口径与灵感工坊 ghibli 先例一致:UI 标签提示「同款气质」,提示词只描述画风特征与世界观、不点名 IP。选包后通用画风方向不再注入(两套硬约束会打架),出片传小朋友照片走图生视频,TA 就出现在那个世界里。

* **数据层**:新表 `birthday_wishes` + `birthday_shoots`(出片盘,懒建/段级参考图/成品链接,同 clips 形态);参考图独立 `birthday/<id>/` 目录 20MB 限量;`occasion` 字段留扩展位(纪念日/告白/毕业后续可共用这条线)。时长 30/60 秒(生日片常比情绪短片长:现场投屏/群发都要有头有尾)。

* **架构**:独立新线(仿宣传片先例),自有引擎 `engines/birthday/` 与提示词 `prompts/birthday.py`;共用确定性件全走 `engines/media/`(切段/锚/SRT/方向目录),已注册进引擎分层门禁 `test_engine_conventions`。

## 2026-08-25 情绪短片出片工作台闭环(minimax H3) + 工坊工作台重构(拆巨石/步骤条卡点) + 视觉打磨

把「提示词复制好就完事」补齐到今天真正在做的收尾闭环:选定本子后按切段建一张**出片执行盘**,从「传角色参考图 → 粘该段提示词 → 生成 → 回填成片链接」全部落在一处,一段一生成,适配 minimax H3 / 即梦这类图文生视频工作流。顺手把工坊工作台从前端与交互上都收了一口:

* **出片工作台**(新表 `clip_shoots`,启动自动建):首次访问按手卡切段**懒建盘**;段号与手卡对齐,手卡改完重算切段后按 index 归并(消失的段忽略、新段无状态);每段最多 3 张参考图,短片参考图独立 20MB 限量。

* **参考图三态**:本地上传(存 `clips/` 目录,鉴权读出)、外链(生图站地址,提示可能带时效签名)、删除(连文件删)。上传/外链/删三处显式 `flag_modified` 落盘——JSON 列原地改 `commit` 不落盘,这是"存了读不出来 404"的根因,已修。

* **成品回填**:每段「出好了」勾选 + 成片链接 + 备注,改完 500ms 防抖整卡回传,进度条实时百分比;权限按用户隔离,删短片连它的出片盘一起清。

* **工坊工作台拆巨石**:`ClipsPage.tsx` 上千行拆成 `panels/clips/` 五个模块(工作台/手卡/出片盘/参数编辑/共用件),与宣传片工坊同构;路由与列表出口保留,小说「投流」页签零改动。

* **步骤条三线统一**:clips 线与漫剧/宣传片共用 StepBar,「现在做哪个按钮」一眼可见。串行流程不套 wb-cols 双栏(没有要随时回看的锚资产,强拆反而多一层空侧栏)——统一的是步骤条心智,不是两栏布局。

* **卡点引导**:点「还没轮到」的步骤,不再静默滚空区,而是提示「先做『当前步』」并自动带你到那步;漫剧/宣传片同样受益。

* **视觉打磨(全走令牌,暗色自动跟随)**:状态徽章语义化(待生成=琥珀 / 候选已出=蓝 / 已选定=绿,时长与画风弱化为灰)、步骤条已完成步浅绿弱底、空态加「场记板+星光」插画、候选行 hover 微抬升、全局 `prefers-reduced-motion` 尊重系统「减少动态」偏好。

## 2026-08-25 小说产线补「返工链路」:概念/架构/大纲/正文四级失效级联 + 本书状态总览 + 重来向导

解决"写了几章发现方向不对,想从架构重新生成却还在老概念上打转、跟旧正文对不上"的老大难。把"换设定"从上到下一级级带透,并让代价在动手前说清:

* **概念可推倒重来**:出方案支持 `avoid`——把当前已定概念当"本轮绝不要的方向"注入重出,不再同输入收敛回老设定;灵感页加「推倒重出一批(抛开当前设定)」。

* **三级失效级联**:概念变更 → 架构标"基于旧概念";架构重写 → 大纲标"基于旧架构"(`projects` 补 `outline_stale` 列,迁移幂等)并对已写正文标失配;重铺蓝图 → 内容变化的章正文标失配。从根到叶逐级带透,不静默错位。

* **本书状态总览条**(项目页常驻):概念→架构→大纲→正文四层进度一眼看完,哪层挂旧版本当场标红 + 给处理动作,点某层直接跳转。

* **重来向导**:状态条常驻入口,对话框选"从哪层重来"(概念最深/架构/重铺蓝图/只重写正文),每种亮清保留/重做与代价账,带去起点;或「清空正文与蓝图重来」(双确认)。

* **确认框算代价**:重新生成架构 / 定为本书概念时,确认框直接显示"将令 X 章大纲、Y 章正文本体标为失配",不再先点再痛。

## 2026-08-25 新增灵感工坊:高辨识度玩法短视频 + 三线提示词细节标准 + 角色定妆卡导出

灵感工坊(/inspire)是「情绪短片」之外的第二个出片入口:不再命题情绪,而是选玩法拍有记忆点的短视频——治愈手绘(宫崎骏那种手绘气质)、黏土定格、赛博雨夜、像素复古、港风胶片、水彩绘本、剪纸皮影、微缩玩具、延时奇观、动物拟人、荒诞脑洞等。走同一套批量产片引擎:三十秒出三个本子三选一,每格带中/英/负面三轨提示词 + 一键复制,拿去 minimax / 即梦 / 剪映直接出片。

* **玩法目录内置强画风锚**:每个玩法都带一段风格化画风描述(描述质感而非点名品牌/IP,规避版权又保辨识度,也不容易往临幕原片跑);玩法仍可与任选画风方向叠加。

* **三线提示词细节标准同步**(漫剧/宣传片):分镜提示词把场景写到「具体空间+关键陈设+相对位置」、光影写清「时段/主光源方向/色温冷暖/明暗对比」、人物定妆到发型发色服装,减少视频模型试错次数,人物一致性靠角色参考图兜底。

* **导出增角色定妆卡**:markdown 手卡带「角色定妆卡」段,每张卡的描述可复制去文生图出定妆图,再上传作参考图。

* 数据层:`mood_clips` 新增 `mode` 列(mood=情绪短片/play=灵感工坊),启动迁移幂等补齐,存量短片默认 mood、行为零变化。

## 2026-08-25 情绪短片工坊大修:方向细化 + 带反馈换批/单条重拍 + 候选看分镜再选 + 手卡可编辑

用户反馈"生成内容总不满意"——排查发现生成方式四个硬伤:候选卡只有摘要、选定前看不到分镜台词(盲选);"换一批"盲抽不带意见;方向只有"主题+画风"两维;节奏铁律只写在代码注释里、模型根本看不见。这轮从提示词到交互整条链路细化:

* **提示词把铁律写进正文**:结构铁律(第一格 2 秒内钩住、每格推进新信息、末格停在金句画面留白)+ 俗套黑名单(雨中痛哭/回头慢镜/牵手奔跑/"如果当初"式字幕)进两段提示词;自定义主题补兜底导演提示(不再裸奔);分镜新增"连续两格不许同景别+同运镜"。

* **方向细化为四个导向维度**:台词风格(旁白独白/对白主导/无台词)、节奏(爆点前置/层层蓄势/结尾反转)、情绪浓度(克制留白/浓烈直给)、氛围关键词自由文本(如「雨夜便利店、暖光」,并进画风卡);全部默认"AI 定",存量零影响。

* **画风目录扩 5 档**:手绘水彩绘本/蜡笔涂鸦/剪纸拼贴/黏土定格/胶片怀旧颗粒(带适用提示),画风选择从下拉改 chips、警示 tip 直接可见。

* **带反馈换一批**:换一批可附一句话意见,连同上一批三条切入的摘要进提示词——这批避开旧方向、落实意见,不再原地盲抽。

* **单条重拍**:方向对但分镜平/台词多时,保切入与画风只重展开这一条(可带意见);重拍已选条会重置三选一,不静默替换手卡。

* **候选卡可展开看分镜**:台词列表+分镜表预览,选定前就能看到真东西;建卡后自动开跑(修掉"提示正在产、实际要再点一次"的断档);生成中离开页面回来能接上进度;生成参数(时长/画风/导向/灵感)建后可改可存,不用删了重建。

* **手卡可编辑**:选定后金句/钩子/台词/分镜字段/三轨提示词都能改,保存按批产同口径归一化并重算切段与警示,候选同步更新。

## 2026-08-25 修「官方 DeepSeek 也又慢又失败」:思考模式默认关 + 渠道参数自动兼容 + 短片生成中防删

线上日志坐实的根因:V4 系模型(`deepseek-v4-flash` 等)思考默认开且 effort=high,我们的结构化长契约会触发数万 token 思考、**吃光整个 max\_tokens** → 正文为空 → 翻倍预算整发重来(思考更长还是空)→ 三轮后报「连续 3 次返回空正文」或「AI 没给出足够的切入方向」。同一个情绪短片提示词实测:思考开约 1 分钟后空正文,关闭后 **6.8 秒**出完整合法 JSON。「接到别的客户端很快」是因为那边要么思考很短、要么默认关了思考。

* **思考模式默认关闭**:适配器新增 `thinking_mode` 控制,全局默认下发 `thinking.type=disabled`(仅对模型名像推理系的下发);设置页每套配置可强制 `low/high/max`(`reasoning_effort`),给想要推理质量的口子。`.env` 可用 `DEFAULT_THINKING_MODE` 覆盖全局默认。

* **渠道不认参数自动兼容**:部分网关对未知参数回 400。遇到 400 且我们发了思考参数时,撤掉参数重发一次,并把该 (渠道, 模型) 记入进程内拒收名单,后续不再下发——不会反复白挨 400,也不会把「Model Not Exist」这类真 400 吞掉。

* **短片生成中拒绝删除**:生成任务是分钟级长活,收尾要 UPDATE 那一行;任务跑着时把短片删了,几分钟批产白跑还崩出一条费解的 `StaleDataError`(线上实测 21 分钟后崩在收尾)。现在 DELETE 对运行中的任务回 409。

* 数据库:`provider_configs` 补 `thinking_mode` 列(启动迁移自动补,存量配置 = 跟随默认)。

## 2026-08-24 修「AI 起名/简介/封面/投稿包不认你设的默认模型」+ 起名改后台任务

一个用户实际撞上的 bug,和它顺手带出来的一条脆弱设计:

* **换了默认模型不生效(403 "Just a moment...")**:书名、简介、封面、主题曲、投稿包这五条线不走任务路由,而是「先取默认档的**协议名**,再按协议名造适配器」。可 cc-switch 之后同一个协议下可以有好几套命名配置(几个中转站 + 官方各一套),按协议名只会取到**创建最早**的那套——于是不管你在设置页把默认改成哪套,请求永远打去最早那个中转站。线上日志坐实:`POST https://muyuan.do/chat/completions → 403`(那套配置的 Base URL 还缺 `/v1`,打到的是网站首页,吃了一张 Cloudflare 挑战页),用户换成官方 DeepSeek 也照旧。现在五条线全部改走任务路由 `get_adapter_for`,那个「只要用来造适配器就一定错」的旧接口直接删掉;`/api/ping-llm` 不指定协议时也改成用「当前生效的那套配置」。新增回归测试专门造「默认那套不是最早那套」的局面(这一格是先前所有档位测试都没覆盖的,所以 bug 才漏出去)。

* **AI 起名改后台任务**:它是这条线上最后一个同步长请求——一轮起名是分钟级的 LLM 调用(慢思考模型更久,遇到空正文还会放大预算重来),同步版把 HTTP 连接一直挂着,家用网络/NAT/代理的空闲超时一掐,浏览器只吐一句 `Failed to fetch`,后端日志里连这条请求都没有。现在和简介一样 `POST` 毫秒级返回 job\_id、前端轮询,单次轮询抖动会自动重试,任务照跑。

* **网络层报错说人话**:`fetch` 对断网、连接被掐、请求超时一律只给一句 `Failed to fetch`,原样上屏没人看得懂。现在分成「请求超时:等了 N 秒没有响应」和「连不上服务器,或连接被中途掐断」两句。

顺带一句给用户的排查提示:OpenAI 协议的中转站,Base URL 一般得带 `/v1`(缺了就是打到站点首页,拿回来的是 HTML 而不是 JSON)。

## 2026-08-24 界面改造成「左侧导航 + 右侧内容」的工作台外壳

之前的样子更像一个"网站":顶栏横排五六个链接,三个工坊(小说/情绪短片/宣传片)里有两个的入口埋在首页标题旁的按钮堆里;进了书,「开书/写作/全书」又是占一整行的三个大 tab,一级导航和内容挤在同一根竖轴上。这次把外壳整个翻了过来:

* **全局左侧导航常驻**(224px):我的小说 / ⚡情绪短片 / 🎬宣传片工坊 / 📖使用指南 / ⚙️设置(管理员再多一项「管理」),当前页高亮一眼可见;三个工坊的入口从此是一等公民,不再藏在首页。

* **侧栏底部常驻任务中心入口与 LLM 累计用量**——长任务在跑时随时能回来看进度,不用记它在哪个页面。

* **项目页改双栏**:左侧「项目 rail」放壹开书/贰写作/叁全书/⚙本书设置(保留完成勾、进度小字与失配红点),开书三步和全书九个页签作为二级项挂在对应区下面;右侧从项目头直接进正文,首屏不再被导航条吃掉一整行。

* **URL 一处未动**(`/project/:id/:zone`、`?tab=` 等照旧),刷新/分享/旧链接跳转不受影响;沉浸模式(F11)与纯阅读页的行为保留。

* **移动端(≤767px)**:侧栏收成 ☰ 抽屉 + 精简顶条,项目 rail 退化为横向折行按钮与横滑页签——组件与桌面同一份。

* 暗色主题跟随现有令牌自动生效;`README` 截图待发版时更新。

## 2026-08-24 一轮代码评审收尾:负面词单点收口 + 空提示词兜底 + 门禁补缝

对 `refactor/p0-hardening` 一轮(八提交)做了全量评审,无 P0;修掉的是口径分叉的温床与守卫被绕过的缝:

* **负面词合并单点化**:宣传片提示词与漫剧预告片各剩一份手写合并没换成 `media.anchors.merge_negative`,已收口;合并时顺手**确定性剔除音频词**(人声/对白/旁白/配乐/music… 一类)——负面词框在各站是给画面用的,LLM 自产的负面词里混进这些词既不生效、还可能被图生视频模型当成"画面里的人"。

* **空提示词守卫被绕过的缝**:切段时若模型漏写某段的运动提示词,画风锚注入会把空串变非空,音频口径的「空提示词不追加」守卫就此失明,最终产出一条只有画风锚+音频约束、没有任何运动内容的提示词。现在先用该段镜头自身(景别/运镜/动作)拼一条确定性兜底,再注入锚;情绪短片与预告片的分镜提示词漏写时同样兜底。

* **通用入口金句占位归空**:提示词里的「(通用入口留空)」常被模型逐字回填,原样会跟着导出手卡印出「金句原句:(通用入口留空)」,归一化时摘掉。

* **门禁补缝**:引擎分层门禁的正则补上 `from app.engines import drama` 这类写法(原先对它失明);资源账本的硬规则(三处注入必须 `exclude_types=RESOURCE_FACT_TYPES`、章后抽取例外)新增源码扫描测试钉住,回归时测试直接红。

* 前端:两处手写 `gen-banner` 换 `ui/Banner`;版面公约门禁补上 `window.confirm(` 写法与 ui/ 导出清单自检;宣传片工作台首屏加载失败不再是死胡同(给重试);「投流」页签与 `/clips` 路由的按需加载真正解耦。

## 2026-08-24 三条出片线的共用件收敛进一处,并加门禁挡住「再抄一份」

三个工坊(漫剧/宣传片/情绪短片)形状很像,于是长出了一批"看着像复用,其实是转引"的代码:宣传片和情绪短片都在跨模块拿漫剧导出器里那个**私有**的 SRT 函数,画风方向目录叫 `DRAMA_DIRECTIONS` 却被三条线一起用,CSV 的 UTF-8 BOM(少了它 Excel 打开就是乱码)各写了一遍。这些都不是功能 bug,但它是 bug 的温床:改一处忘另一处,导出的字幕就和剪辑清单对不上。

这次把真正共用的三件挪进 `engines/media/`,并把依赖方向钉死:

* `media.subtitles`:SRT 时间码与累计时间轴(有台词才有字幕条,空台词的格时长仍计入时间轴——否则后面的字幕整体前移)。三条线同一根轴。

* `media.directions`:画风方向目录改名 `DIRECTIONS`,不再姓"漫剧"。加一档画风,三条线同时多一个选项。

* `media.text`:`clip` / `coerce_int` 脏值收敛,以及 `csv_text`(它统一负责 BOM)。

* **依赖方向单向**:三条线都往 `media/` 看,`media/` 是叶子;**三条线之间不许互相 import**。

* 新增门禁 `tests/test_engine_conventions.py` 扫源码把这四条判据钉住(和前端那条版面公约门禁同一个思路,**同样没有豁免清单**):被拦住时只有两条出路——把共用件挪进 `media/`,或者证明判据本身写错了。

对使用者没有行为变化:三条线导出的 SRT 时间码、CSV 的 BOM 与首行、手册内容本来就有测试逐条钉着,这次搬完 656 条后端测试全绿。

## 2026-08-24 角色资源账本:谁手上有什么,不许凭空掏出、用完要收口

长篇写到几十章,读者最先发现的破绽往往不是人设崩,而是**东西对不上**:第 12 章凭空掏出一把从没提过的匕首、第 3 章送人的玉佩第 20 章又戴回来、干粮吃完三章了还写着"摸出剩下的半块"。这些事实(`持有…` / `会…`)其实早就在故事圣经里,只是混在"人物当前状态"里一起注入,治不了。

现在它们有了自己的账本:

* **闭集红线**:账本列清在册的关键道具与本事,并立三条硬规则——不许凭空掏出(本章蓝图【关键道具】点名的不算)、新增必须在正文当场交代来源(谁给的/哪儿捡的/怎么换的)、用掉送出损毁要写明。角色那边早有"已登场角色名册"防凭空冒人,资源这边一直是空白。

* **不再被挤掉**:硬约束块 40 行满了先砍 `minor`,而"持有"往往就是 minor——账本另有一份 24 行预算,`critical` 永不被砍。

* **能收口**:章后抽取现在要求"失去"也报一条(不是只报"获得");旧事实的 `replaces` 匹配放宽到三级(精确 → 去空白 → 唯一子串),**没匹配上会留日志而不是静默跳过**。以前模型多写一个括号补注,那条旧事实就永久挂着,后面每章都把作废的事实当硬约束注入。

* **门禁也看得见**:一致性检查新增"角色资源"维度,对着同一份账本查"凭空掏出/已失去又出现/用掉没写明"。

* 故事圣经看板给资源行标了「持有」「会/能」badge——账本失真这种事,最后还得人眼一扫认出来。

## 2026-08-24 三条出片线补上「声音怎么来」:分轨,不是静音

AI 视频现在自带声音了,那还要配音稿干什么?——因为**声音得分轨**,这一条以前只在脑子里,现在写进了提示词和手册:

* **环境音/动作音留给视频模型**(风雨、脚步、器物、灶上爆炒):这是它的长处,生成时别关。

* **人声(解说/台词)自己配一条整片的**:模型自带的人声不保证一字不差念你的稿(slogan 与金句会被改写,情绪短片的金句溯源就白做了),而且分段生成、逐段拼接时,每段自带人声的音色语速在接缝处全跳。

* **BGM 按段情绪词整片铺**:分段各自带音乐,段边界必然错拍。

* 所以三条线的视频提示词末尾统一追加一句「只要环境音,不要人声与背景音乐」——它是给 Veo 一类**自己会编对白**的模型看的;导出手册里也把三轨的来路写清了。

* **例外照实说**:整片就一段、一次生成完(常见的 15 秒情绪短片),没有拼接错位问题,**直接用模型自带的音频最省事**,手册这时会自动换成这套口径。

* 音频词只进提示词正文,**不进负面词框**——那个框各站都是给画面用的,塞「人声」既不生效还可能干扰画面。

## 2026-08-24 不止改编小说:新增「宣传片工坊」与「情绪短片工坊」,从一本书长成一个视频制片台

写好的书能变漫剧,现在不写书的人也有得玩了——两个新工坊共用漫剧那套制片管线(锚段一致性/三轨提示词/切段/SRT),但各自解决一类新需求:

**📣 宣传片工坊(书房入口,城市/景区/品牌)**

* **先聊后做**:与 AI 策划总监多轮研讨(真流式打字机)——你说「想从吃的入手」,他反问关键问题、给具体建议、每轮复述共识;聊透了一键**收敛成创作简报**(定位/受众/段落结构/slogan/⚠需人工核实清单),简报是后续一切生成的契约,可锁定可重收敛,不再「一版定稿不对就推倒重来」。

* **素材点事实红线**:史实/数据/slogan 只可引用你给的素材点,拿不准的自动进「需人工核实」——宣传片把年代写错就是事故,这条纪律是底线。

* 全链路:视觉风格+地标卡(锚段一致性)→ 解说词 → 分镜 → 三轨提示词 → **生成切段**(按你的画布拼接工作流:镜头边界并段 ≤5/10/15s,每段整段视频提示词+首帧指引+拼接提示)→ 成片包(配音稿/剪辑清单)→ 手册/CSV/SRT/JSON 导出。

**⚡ 情绪短片工坊(双入口)**

* **通用入口(书房)**:15/30 秒情绪命题短视频——遗憾/争吵/爱情/童趣/思念/孤独/治愈/热血/告别/重逢十大命题(每个带导演提示),一次产 **3 个不同切入的本子**三选一:钩子开场→情绪蓄势→金句字幕卡收尾,每本自带三轨提示词/切段/SRT。批量试号逻辑,不磨单版。

* **小说衍生入口(全书→「投流」页签)**:从你的定稿章节里抽金句与名场面出投流种草片——**金句溯源校验**:每个本子必须给出金句在正文里的原句,引擎逐句比对,编造的引用当场标红。这是「和 DeepSeek 聊着写」给不了的:它读的是你的书,引用的是你写的话。

## 2026-08-23 漫剧打通「怎么变成视频」这一步:每格多一条运动轨,分镜按你那家站的单次上限自动并成视频段,出好的图挂回那一格

有用户问:「这些生成视频的网站一次最多只能出 15 秒,咱们出来的这个是剧本吗,我该怎么用?」这问得对——我们以前只把**出图**那一半讲清楚了:提示词、粘贴版、定妆照都有,但**从静帧到视频**那一步全靠你自己琢磨。而真实做法是「**一段生成一次(5-15 秒),再在画布/剪映里首尾相接**」,我们的分镜格却是 2-8 秒一格,几十格一格一次既贵又碎。这一版把这一步补齐:

* **每格多一条「让这一格动起来」(图生视频提示词)**:中英文各一条,一键粘贴。它**刻意只写怎么动**——运动、幅度、运镜、时长,**一个外貌/服饰/画风词都不写**。原因是图生视频的第一大翻车就是「提示词里又描述了一遍长相」:首帧图已经把脸钉死了,文字里再写一遍,模型就照文字把脸重画。这两栏也都能手改,改完点「保存」。

* **视频段计划:替你把格并成段**(分镜表最下面,不调 AI、即时算)。先点你那家站的单次上限 **5 / 10 / 15 秒**,表里每一段直接告诉你:**首帧用哪一格的静帧、这一段怎么动、几秒、要生成几次、字幕是哪句**,每段自带一键粘贴。并段规则严格到宁可多生成一次:**同一场景、不引入新角色、不超上限、一段最多一条台词**——多一个人首帧图里就没有他,两句台词字幕与口型必然对不上。换上限即时重排,嫌段太碎就调到 15 秒。

* **超上限的段如实标出来,不假装一次能出**:整段标红并写明「**要生成 N 次**」,给出接法(第一次用首帧出满上限,之后每次拿上一次的**尾帧**当首帧接着出),也提示你可以回分镜表把那格拆短。这个「N 次」同时就是**你的实际计费次数**,一眼看得出这一集要跑多少段。

* **文生视频也给了,但把风险说在前面**:没有首帧的站(Sora / Veo / 可灵文生)必须**自带外貌与画风锚**,所以这一版的 t2v 粘贴版是「出图提示词 + 运动句」拼起来的完整一条。同时明说:**有人物的格别用它,同一个角色每段都可能换脸**;空镜/氛围格才划得来(能省掉出静帧那一步)。

* **出好的静帧挂回那一格,做完打个勾(新)**:每格提示词下面多了一条「**这一格出好的静帧**」——在生图站出好的那张,上传或贴外链挂回来(单张 4MB,一格最多 2 张,常出两版挑一版)。挂上就**自动勾上「静帧出好了」**,视频段计划里那一段立刻变「**首帧图已就位**」;删到一张不剩,勾会自动撤掉——绝不显示「做完了」而其实没有图。旁边两个打勾栏(静帧 / 视频)**点一下就存**,不用再点保存;集内进度(静帧 N/M、视频 N/M)顶在「三轨提示词」那一行,每格标题上也有「静帧✓ / 视频✓」,几十格一格格翻也扫得出做到哪儿了。

* **视频段计划标出「这一段能不能开工」**:段表多一列**首帧图**(✓ 已挂 / 待出图),总览里写明「首帧图已就位 N/M 段」,没挂的段直接告诉你该先出哪一格。**首帧一律取段首格**,挂在段里第二格不算——并段后几格共用一张首帧图,这是图生视频的规则,不是我们的选择。

* **视频文件刻意不收上传**:一条几十 MB,一集就能吃满整个项目的上传配额,而剪辑本来就在你本机做。所以那一格只填「**成片在哪**」(文件名/目录/外链),它会一起写进导出的施工单,剪辑时照着对号。

* **导出的 CSV 变成「逐格施工单」**:一行一格,把**段号 / 段内哪几格 / 段时长 / 要生成几次 / 该段的图生视频提示词**都并进去,再补上**你挂的静帧、成片在哪、两个打勾栏**——站里勾过的导出来就是勾上的(导出还要重勾一遍等于白勾),没做的留空给你手打。导出的手册(Markdown)开头也多一行**施工进度**,段表带首帧图那一列;几十格的手工活没有进度表,必然做丢或重做。导出的手册(Markdown)里也多了一节「让它动起来:视频段计划」,段表 + 每段三份粘贴版(中文站 / 英文站 / 文生),离开本站也照着走得下去。

* **顺手修掉一个越权漏洞**(写这一版的护栏时被自己的测试抓到):漫剧的**集详情 / 删除集 / 改分镜 / 导出手册**等十来条接口只校验了登录、**没校验项目归属**——知道 id 就能读、能改、能删别人的剧集。已把归属校验下沉到取子资源的公共入口(集 / 分镜 / 角色卡各一处),漏一条的可能性从此消掉,并加了一条专门的越权回归用例(别人一律 404)。

* **护栏与说明**:新增 **40 条后端用例 + 5 条前端用例**钉住这一层——i2v 正文里外貌词一个都不许出现、t2v 必须自带锚、运镜中英对照、模型漏给运动轨时的兜底、并段四条规则各一例、超上限如实标注、换上限即时重排、挂静帧自动打勾 / 删空自动撤勾、首帧只认段首格、上传只收真图且文件名一律服务端生成(定妆照与静帧同目录不许撞号)、越权 404、上传走 multipart 且不许手设 Content-Type。工坊引导与「出片指引」也同步改写,把「先传首帧再粘提示词」「换脸八成是粘错了提示词」「N 次怎么接」「首帧图还没挂该先做哪一格」写成人话。

## 2026-08-23 修「女角色的提示词生成成男的」——性别升级为一栏可拍板的硬约束,还能单张重出

有用户反馈角色卡生成出来,**女角色的外貌/声线被写成了男的**,而且「没找到单独更改的地方」。查下来是我们的问题,而且是三层叠加的:① 提示词里**零性别约束**,三个示例还全是男性(剑眉入鬓 / 玄色劲装 / 青年男声)——模型没线索时就照示例写;② 模型其实**常常看不到性别**:角色档案摘要只截 300 字,「她」「夫人」这些唯一的线索正好被截掉;③ 卡上没有性别这一栏,错了只能改自由文本,**英文那条(生图站真正吃的)还改不到**,而且想修一个角色得整批重跑。这一版一层层钉住:

* **性别成为卡上的一栏,你点一下就拍板**:女 / 男 / 其他 / 未定,点完立刻保存。**用户拍板 > 档案推断 > 模型自填**,你定了的性别之后谁也改不动。

* **从档案里找性别,找不到就说「未判明」,绝不硬猜**:判定只认档案里真有的线索(代词、称谓),范围也从 300 字摘要放宽到**姓名 + 别名 + 完整档案 + 最近 40 条事实**,并把依据一起写给模型(「依据:档案里出现『她』」)。线索打平(如**女扮男装**)或压根没有,就明说「未判明,你按档案判断」——宁可交给模型和你,也不瞎定一个。

* **性别一路带到底**:角色卡提示词把性别列为**第一条硬约束**(并明说「女角色不许写成剑眉入鬓 / 喉结 / 青年男声」),中文外貌开头必须先写性别与年龄段、英文关键词必须以性别词开头;定妆照提示词、每一格分镜提示词、声线选型也都照卡上的性别写。连模型没给、由引擎兜底拼的提示词,中英文都会带上性别词——**没有一条路径能把性别丢掉**。

* **「重出这张卡」(新)**:只重出这一张角色卡,按你拍板的性别重写外貌 / 服饰 / 声线,不用整批重跑;显式点它就是要覆盖,**连锁定的卡也照重**(锁定状态不变)。模型这次输出被截断,报错会说清是截断,**原有描述一个字都不动**。

* **卡上每一栏都能直接改了**:除锁定外貌段与声线,新增**英文外貌关键词**与**标志服饰**两栏可编辑(以前只能改中文那条,英文轨改不到——这正是「改完还是男的」的原因),各带一键复制,卡上也写明「每一栏都能直接改,改完点保存」。

* **描述与性别打架会当面提醒**:标了女却写着「少年 / 剑眉入鬓 / young man」时,卡上浮出一行「⚠ 描述里…与标定性别不符」。**刻意不自动改你的字**——女扮男装是正当写法,只有你知道该改哪边;「男装」这类也刻意不算冲突。

* **护栏**:新增 11 条后端用例钉住全链路——判定只信真线索、档案压过模型说反的答案、PATCH 只收 female/male/other/空(「女神」一律 400)、单卡重出只问一次且带硬约束、截断时不写坏原卡、别人的卡重出 404、定妆照与分镜兜底都带性别词。

## 2026-08-23 修「复制失败,请手动选中文本复制」——凡是要你复制的地方,现在真的一键就能复制

有用户点「复制整段提示词」被顶回一句「请手动选中文本复制」。这不是浏览器不给权限,是 `navigator.clipboard` 这个 API **只在 https / localhost 下才存在**;用 `http://某地址:8080` 打开本站时它压根没挂上来,一调用就抛异常。也就是说:**在线试用地址上,这颗按钮以前基本注定失败**。修法:

* **复制改成三层兜底,全站统一一份**:① 有 Clipboard API 就用;② 没有(正是 http 页面的情形)就退回临时文本框 + `execCommand("copy")`,HTTP 下照样复制得动,移动端与 iOS Safari 的选区怪癖也一并处理;③ 万一两层都被拦,弹出一个**已经替你全选**的文本框,按 Ctrl+C(Mac ⌘+C)就走,还能点「再试一次」——**不再有把人堵死的死胡同**。

* **该有复制按钮的地方补齐**:漫剧的只读提示词框(一键粘贴整段/负面词、预告片各格提示词)点一下即**整段全选**,想手动复制也只需一步;后台的注册邀请码多一枚「复制码」按钮(这东西本来就是要发给别人的)。投稿面板与漫剧面板此前各写了一份复制逻辑(一份有兜底、一份没有),现在合并到 `ui/copy`,不会再出现「这个面板能复制、那个不能」。

* **护栏**:新增 7 条前端用例钉住三层行为——有 API 走 API、无 API 回落、API 抛异常回落、两层皆败才弹兜底层且文本已全选、成功时不弹任何东西。

## 2026-08-23 修「出定妆照提示词失败:模型返回的角色名对不上」——真凶是输出被截断,不是名字

有用户反馈「出定妆照」经常失败,报「这轮没出结果,请重试(模型返回的角色名对不上)」。查了线上日志,**这句话是甩锅**:真正的原因是模型的 JSON 被 max\_tokens 砍断(日志里是 `Unterminated string ... char 73`),一批 8 条只写完两三条就断了,而我们把整批半截结果全丢掉,还提示你去查角色名——照那个提示排查,永远查不出问题。这一版从五处一起治,让这个按钮**不再靠运气**:

* **截断也算失败,自动放大预算重来**:以前只有「正文完全为空」才会放大输出预算重试,正文吐了个开头被砍断则被当成成功、半截内容一路流到解析失败。现在截断同样触发翻倍重试(最多 3 轮);三轮都断就把最长的一次交给下游抢救,而不是整次调用白跑。资产卡类任务的输出预算也从 5000 提到 8192(推理模型的思考要吃掉一大截)。

* **一次少要几条,不赌长输出**:定妆照改成**每 4 位角色一批**分开调用(原来一次要 8 条,正是被截断的原因),提示词里也明确要求「宁可紧凑写完,也不要长到只写完前几条」。

* **半截 JSON 也能抢救**:新增「按花括号配平逐条抢救」的解析——写完的条目照样收下,只丢最后那个没写完的;顶层写成数组、围栏里混了解释文字也一并兼容。

* **名字宽容匹配**:模型把 name 写成「【沈砚】」「沈砚(主角)」「沈 砚」,或把键名写成 `prompt_cn` / `中文` / `角色`,现在都能对上卡(原名 → 归一 → 唯一包含,逐层退)。只要一张卡时,名字写跑了也照样收下;**多条时刻意不按位置硬配**——把甲的脸描述写到乙的卡上你看不出来,却会毁掉整片的人物一致性。

* **还是没给?引擎自己拼,按钮永不走进死胡同**:仍缺的角色先单条重问一次,再不给就由引擎按「构图规范 + 这张卡的外貌锚 + 画风锚」确定性拼一条——这条提示词的成分本来就是死的,不需要创作,拼出来照样直接能用。toast 会**如实告诉你**「其中 N 张由引擎拼好」,想换写法点那张卡的「重出提示词」。

* **真失败时的报错说人话**:改为点名到底哪一环坏了——是这些角色卡还没有「锁定外貌」段(去点「生成资产卡」)、是模型没吐出可用结果(多半截断,建议调大 max\_tokens 或换个思考更省的模型),还是它返回的名字确实对不上(把它给的名字与本书角色名一起列出来)。

## 2026-08-23 漫剧提示词「一键粘贴」按你用的生图站适配(只有一个描述框的站也能一段粘完);角色加定妆照,锁脸不再靠运气

拿到提示词之后最卡人的两件事:一是**提示词有正文、有负面词、还有比例,到了生图站不知道该往哪儿粘**——很多站(GPT-image / DALL·E / 豆包 / 通义)压根只有一个描述框,没有负面词框;二是**人物脸对不上**,同一段外貌描述连出十格,出来十张脸。这一版两件一起治:

* **一键粘贴,三种站各一份**:每格提示词下面多一个「你用的生图站」下拉,选定后一枚按钮整段复制,选择会记住。① **只有一个描述框的站**:负面词自动改写成「【不要出现】…」并入正文,比例、构图也一并写进去,**整段粘一次就行**,不用你自己拼;② **有负面词框的站**(即梦/可灵/SD/ComfyUI):正文与负面词分两枚按钮,各粘各的;③ **Midjourney / Niji**:英文提示词 + `--ar 9:16` + `--no` 排除项已经拼好。三份都由后端同一套规则生成,所以**站内看到的、导出手册里的、CSV 里的完全一致**,不会两处对不上。

* **定妆照(新)**:角色卡上多一步「出定妆照」——AI 为每个角色写一条**参考图专用提示词**(单人正面半身、纯色背景、柔和均匀光、3:4,带本书画风锚),你去生图站出一张满意的正面像,**上传回角色卡存着**(也可以只贴外链)。之后哪一格有这个角色出场,那格的粘贴版会自动多一句「【参考图】用『沈砚』的定妆照」提醒你上传参考图——**光靠文字描述压不住漂移,参考图才真锁脸**。没上传定妆照的角色不会硬塞这句废话。

* **只补缺,不糟蹋你改过的**:批量「出定妆照」只给还没有的角色生成,已经有的一个都不动(全都有了就直接说「都已经有了」,不白跑一次 AI 也不白花钱);想重写某一张,点那张卡上的「重出提示词」显式覆盖。参考图提示词跟锁定外貌段一样可以手改,改完照样受「🔒 锁定」保护。

* **手册也跟着改了**:导出的 Markdown 里,角色卡多一节定妆照(提示词 + 已上传几张),每格提示词按 ①单框站 / ②双框站 / ③Midjourney 三档列全;CSV 多一列 `paste_oneframe`(整段版),批量出图直接按列复制。出片顺序那句改成「先出角色定妆照,再逐格出图」。

* **上传是按文件内容判定的**:只收真的 PNG / JPG / WebP(改扩展名没用),文件名由服务器生成、不用你给的路径,单张 4MB、一个角色最多 3 张、单项目总量 80MB;图片读取走登录鉴权,别人的项目一律看不到。

* **修:删掉一本书,它的漫剧资产以前留在库里**。删项目时只清了正文/大纲/圣经那几张表,漫剧的风格卡、角色卡、场景卡、集、分镜、成片包、预告片七张表**一行都没删**;上传的定妆照文件也留在磁盘上,白占那本书的 80MB 配额。现在删项目(以及后台删用户)连漫剧资产与图片文件一起清干净,并加了回归测试。

* **引导同步更新**:工坊引导卡的 ② 角色卡这一步补上定妆照,出片指引第一步改成「先出定妆照」,并把「哪种站选哪一项」写清楚;排障表新增「出定妆照说要先定画风」「上传被拒是为什么」。

## 2026-08-23 生成时能看见 AI 正在写的字了(实时直播窗);顺手治了「模型连续 3 次返回空正文」

以前点下「生成」,页面只有一句「正在写草稿」,几十分钟看不到一个字——不知道它在写什么、写到哪、是不是卡死了。这一版把模型正在吐的字**原样接到前端**:

* **实时直播窗**:任何后台任务(写章节、架构、蓝图、摘要、一致性、定稿、翻新、漫剧……)运行时,右下角浮出一个小窗,逐字滚出模型此刻正在写的内容。**一步一屏**:换到下一步(如「2/4 角色动力学」)自动清屏,不把几段正文糊在一起;同一步里的进度计数(如蓝图「已生成 12/40 章」)只换标签、正文照旧往下滚。窗口可收起、可关掉,关掉不影响任务。

* **断线能续看**:切走页面、锁屏、网络抖一下再回来,直播从上次看到的位置接着走,不重复也不跳字;落后太多(缓冲已滚过)会明说丢了多少字,而不是假装连续。长任务(连写几小时)也不会中途「假装结束」后再不更新。

* **修「模型连续 3 次返回空正文」**:同一个模型在别的客户端里好用、在这里频繁报空正文——根子是**推理模型的思考内容此前被整段丢掉**,既拿不回正文,也说不出为什么空。现在:①默认走流式,思考与正文分开收;②模型把答案整段塞进思考字段时**兜底取出来**,不再让一整章白跑;③报错说人话并给出下一步——是**思考吃满了输出预算**(自动放大预算重试,最多 3 轮)、还是**被安全策略拦下**(放大没用,一次就报)、还是**中转渠道空转一个字节都没吐**(别再去查 key);④思考被截断的半截内容**绝不**当正文写进书里。

* **顺带更快**:流式优先意味着第一个字来得早,长章生成的「静默等待」明显变短。

* **修(本次自查发现)**:改稿讨论、架构研讨、润色讨论、灵感对话这四处多轮对话入口,空正文时的「放大预算重试」在改造中失效了(会把第一次空正文直接摔给你),已归拢到同一处共享逻辑并加了回归测试。

* **修**:手机上直播窗压住了写作区底部的 AI 输入条(AI 点不开),现在直播窗上抬,给输入条与安全区留位。

## 2026-08-23 漫剧工坊「不知道该点哪儿」这件事:每一步都有人告诉你;另修三处真会丢东西的毛病

漫剧工坊功能是齐的,但进去容易发懵——按钮一排,不知道先点哪个、哪个是灰的、拿到手册之后又该干什么。这一版补上全套引导,并修掉三处会让你白忙一趟的毛病:

* **进门就知道怎么走**:顶部新增引导卡——这是什么(本站只出**拍摄手册**,不出图不出视频)、①→⑤ 照着走每步在干什么、以及「按钮点不动/报错了」的对照表(「拆分镜」是灰的 = 这集还没剧本;提示「先生成美术风格卡」= 回 ① 定画风……)。看烦了可以收起,想看再点回来。

* **「现在该做这一步」**:管线步骤条从「点亮即完成」升级为三态——✓ 已完成、▸ 你正卡在这、未开始;下面直接一句话点名下一步该点哪个按钮,右边跟一枚「去这一步」。展开某一集时同样有一句状态提示,四个流水线按钮鼠标悬停都会说明「为什么现在能点 / 为什么不能点」。没选集时不再是一片空白,而是提示「点任意一集展开单集流水线」。

* **出片指引(手册之后怎么做)**:底部新增一块——出图(提示词粘进即梦/可灵,负面词带上,9:16)→ 图生视频(按分镜表的运镜与秒数)→ 配音(按成片包配音稿,音色看角色卡建议)→ 剪映拼装(照剪辑清单摆片段、导 SRT、按 bgm\_tag 铺乐)→ 成本参考(同类开源项目自报一集约 ¥130,视频生成是大头;以平台实际计费为准),末尾附配音与商用授权的合规提醒。

* **修:数章并一集会悄悄丢正文**。AI 切集时允许把过渡的几章并成一集,但之前每集只记得住一个源章号,写剧本时只读了那一章——被并进来的章节内容凭空消失。现在一集完整记下它的**所有源章**(集列表里显示「第 3-5 章」这类标签),剧本按全部源章的正文写;老数据自动迁移,不用重切。重新切集时的「会覆盖已有集」判定也跟着改准了。

* **修:分镜格数被硬砍在 24 格且不吭一声**。90 秒一集本来就该有三十几格,之前一律截到 24 格、还不告诉你,长集就成了残的。现在**上限按这一集的目标时长算**(短集不虚胖、长集放得开),真被截断会明确显示一条「分镜说明」,写清截了多少格、总时长比目标短多少——宁可说难听话,不装作正常。

* **新:单格重出提示词**。某一格的提示词不满意,不必整集重跑(慢,而且会盖掉你手改过的其它格)。那一格上直接「重出这格」,可以补一句要求(比如「换成俯视、雨更大」),只重出这一格;画风锚与角色锚照旧逐字注入,不会因为单独重出就漂。

## 2026-08-23 修漫剧工作台左侧资产栏的挤压换行:侧栏排版整体翻新

桌面端漫剧工作台的左侧资产栏(风格卡/角色卡/场景卡)在窄窗口下标题与按钮挤在同一行、文字被排挤得难看。这一版把侧栏排版整体翻新:侧栏加宽,标题独占一行(副标题另起一行小字),操作按钮收成两枚一行的整齐网格,方向选项缩小一号——不再有文字被挤压折行的位置。另:自部署服务器(Docker)不会随仓库更新自动重建,漫剧功能要 `git pull` 后 `docker compose up --build -d` 重新构建才会上线(官网与桌面安装包是自动的,服务器不是)。

## 2026-08-22 漫剧画风方向自己拍板:AI 先荐三个(带理由),真人/二次元/国漫任选;配音立合规红线

* **画风方向(新)**:生成美术风格卡前先选「方向」——**国漫厚涂 / 日系二次元 / 3D 动画 / 水墨国风 / 赛博霓虹 / 真人写实 / AI 按书定**。方向是硬约束:风格卡锁定段、每格分镜提示词全部落在所选方向内,选了水墨就不会蹦出赛博霓虹;换方向重生成风格卡即可全片换皮。**真人写实保留但挂警示**(AI 真人目前仍有恐怖谷痕迹、跨镜头一致性更难),动画系为默认推荐——这也是当前 AI 出片质量的真实水位。

* **方向推荐**:不确定选哪个?「AI 荐方向」按本书题材/基调/场景推荐前三个方向,按优先级排序、每条带扣题理由(第一名标 ★),点一下即选中;推荐只是建议,随时可以无视自己挑。

* **配音合规红线**:声音受法律保护——声线选型新增硬规则,**绝不指名或暗示模仿任何真人/演员/声优的声音**,只用音色特征描述(性别/年龄段/音色气质);角色卡区常驻合规提示:用平台**正版音色库**、切勿克隆他人声音、商用发布前确认所选音色的**商用授权**。

## 2026-08-22 写长了不再「前后像两本书」:给整本书立一部宪法,AI 全程照着写,写完还要对一遍账

写到三四十章,最常见的崩法不是文笔变差,而是**前后像两本书**:第一章说这院子空无一人,第八章冒出个「素来伺候起居」的老仆;上一章「四下死寂、连鸟都没有」,下一章主角被鸟叫吵醒;开篇立下「三十一天期限」,写到中途谁也算不清还剩几天;好不容易埋下的「系统」,连着七八章再没出现过。根子在于:这些「整本书都成立」的东西只写在设定里躺着,早章一滑出上下文窗口就再没人管。这一版把它们立成一部**故事宪法**,每章照着写、写完自动对账:

* **故事宪法(设置区新卡)**:把「全书恒真」的三样东西单独立出来——**刻意留白**(这本书永远不解释的事,比如「父亲为何离家」)、**常驻装置**(系统 / 契约 / 贯穿全书的某件物品,可设复现节奏与重要度)、**倒计时**(名称 + 总天数,比如「离考试还有 31 天」)。这三样连同世界观硬规则合成一张宪法,生成每一章时全程注入;更关键的是**写后的一致性门禁也终于看得到它**,不再是早章立的规矩后面无人执行。

* **AI 反过来帮你补宪法**:写着写着,AI 从正文里认出「这条像是本书的恒真设定」,会在问题清单里提一条建议,你点\*\*「采纳进宪法」\*\*才真的写进去——不点就只是建议,它绝不自动改你的设定。

* **人物不再凭空常驻**:每章生成时带上一份\*\*「已登场角色名册」\*\*,AI 只能从名册里挑人写「素来相熟」;门禁也新增一维,专揪「某人第一次露面就被写成早已常驻」,正常的初次登场不误伤。

* **环境不再穿帮**:同一场景接着写、中间没有跳时,声音 / 光线 / 天气凭空翻转会被当场拦下(就是「死寂无鸟 → 被鸟叫吵醒」那种)。

* **倒计时算得清了**:每章章末记下「故事第几天 / 正文明说还剩几天」,系统据此算出一条**权威天数轴**——天数倒流、剩余天数反而变多、和正文口径不符、期限已过还在倒数,这四类矛盾会被指出来给你看。

* **常驻装置不会写着写着消失**:设了「系统」这类装置,AI 会按你定的重要度被催着让它按节奏露面;真断档太久,问题清单中会出现一条「常驻装置」提醒(只提醒,不拦你的章)。

* **顺手治一条 AI 味老套路**:「他似乎想起了什么,却又抓不住」——这种挑逗完记忆又落空的四段式套话,现在会被去 AI 味门禁扣分并要求改写。

## 2026-08-22 漫剧预告片一键混剪 + 工作台改版:管线步骤条 + 资产侧栏,像一个真剧组了

* **预告片(新)**:从各集的钩子/卡点/高能分镜里,一键混剪一条 30-60 秒宣传片。AI 按预告片行规办事——炸点开场 → 世界/人设速览 → 冲突升级连切 → 悬念定格;产物含「文案骨架」(旁白 + 金句)、混剪分镜表(每格标注取材自第几集,新创镜头单独标)、三轨提示词(画风/角色锚照旧逐字注入,LLM 漏了引擎兜底);可导出拍摄手册与 SRT 字幕。预告片不剧透关键反转,只放大悬念。

* **工作台改版**:原来的竖排长页升级为「剧组工作台」——顶部**管线步骤条**(风格卡 → 角色卡 → 切集 → 单集流水线 → 预告片,点亮即完成,点按即跳转),桌面端左侧**资产侧栏**常驻(风格卡/角色卡/场景卡随取随改,sticky 不随滚动消失),右侧主区专注切集与单集流水线;移动端自动回落单列,顺序就是工作流顺序。

## 2026-08-22 漫剧工坊补齐出片最后一块:声线选型 + 成片包(配音稿/剪辑清单/SRT 字幕)

分镜提示词管「画面」,这一版补上「声音」和「剪辑」,从「一堆素材」到「照着拼就能出片」:

* **声线选型卡**:角色卡一键补 TTS 平台选型建议(剪映文本朗读 / 火山引擎 / MiniMax 各给方向)与朗读指示(语速/情绪基调/重音)——拿着它去任何配音工具都能挑到接近的音色,角色之间声线拉开区分度(听声辨人)。

* **成片包(每集一键)**:①**配音稿**——逐镜头的朗读文本(数字口语化、去画面注释的 TTS 友好版)、发音人、声线、估时,配音估时与画面时长并排,长短不匹配自动提示「延长镜头或提语速」;口播解说模式另给整段口播一把梭版。②**剪辑清单**——镜头顺序/时长/字幕/转场建议/配乐情绪标记的时间轴,配乐按情节段落打标,转场服务叙事(冲突快切、落点叠化、悬念黑场)。

* **SRT 字幕导出**:按分镜时长确定性生成标准 .srt 时间轴(有台词的镜头才有字幕条),剪映/PR 直接导入,不用手压字幕。

* 导出矩阵补齐:拍摄手册(md)/ 分镜表(csv)/ 全量(json)之外,新增**成片包(md)与**字幕(srt);出片顺序一句话写在成片包末尾——出图 → 图生视频 → 配音 → 按清单拼 → 压字幕 → 铺 BGM。

## 2026-08-22 小说写完还能变成漫剧:新增「漫剧工坊」,四步产出能直接出片的拍摄手册

写好的小说不止能投稿——这一版在「全书」区新增**漫剧**页签,把已定稿章节改编成竖屏漫剧的完整拍摄手册,拿去即梦/可灵/Midjourney/剪映直接出片。沿用「只产提示词」哲学:不接生成模型、零新配置、零新依赖。

* **① 美术风格卡**:按本书类型定一段「画风锁定」(媒介/笔触/色彩/光影),之后每一格分镜提示词都逐字嵌入这段锚——上百格画面画风不漂移。

* **② 角色卡 / 场景卡**:从故事圣经批量生成「锁定外貌段」+ 配音声线,角色出场的那格逐字注入外貌锚,同一个人不换脸;手动调过的卡可**锁定**,批量重跑不覆盖。

* **③ 集数规划**:选已定稿的章节范围,按短剧节奏切集(默认约 90 秒/集,对白演绎 / 口播解说双模式),每集独立小冲突 + 开场 3 秒钩子 + 结尾卡点;重新规划只替换所选范围,范围外不动。

* **④ 剧本 → 分镜 → 三轨提示词**:单集剧本 → 分镜表(场景/角色/景别/运镜/台词/时长)→ 每格三轨绘图提示词(中文六层 / 英文 MJ 版 / 负面词),LLM 偶发漏注入锚段时引擎兜底强制拼入;分镜与提示词逐格可手改,一键导出整集拍摄手册(Markdown / CSV / JSON)。

一致性是漫剧的命门:画风锚、人物锚、场景锚全部由资产卡统一管理、逐字注入每一格,而不是各写各的——这正是"拍出来像一部剧"和"一百张拼贴"的区别。

## 2026-08-21 章被系统拦下时不再满屏黑话:收成一张说人话的卡,三个按钮讲清怎么办

章写完却被一致性门禁拦下(和前面的设定撞了),以前满屏是「放行 / 契约重提 / 重检 / 按建议修订」这类黑话,还散在好几处,到底点哪个全靠猜。这一版把「章被拦了怎么办」收成**一张卡**:

* **一句人话说清、三个按钮到底**:卡片先用一句话讲清这章为什么被拦、和哪条设定撞了,然后只给三个选择——**让 AI 按这条重写** / **去改设定** / **就这样,忽略继续**;下面另有一条不起眼的「改好设定了?重新检查」。原来十几个黑话词,收敛成三个动作,点哪个都不会错。

* **三处都是同一张卡**:写作页章首、生成结果卡、翻新「重度重写」被拦时,弹的都是同一张卡、同一套话术,不再一处一个说法。能力一个没少(放行、按建议改、重新抽契约都还在),只是不必再懂黑话。

* **顺手修个坑**:批量「重度重写」时,某章重写完撞了设定被拦,以前会被**当成功悄悄跳过**(其实没进圣经/摘要,后面的章会踩到旧数据)。现在如实停下报错,并给一个「去处理第 N 章」直接跳过去用上面那张卡解决,处理完回来接着续跑剩余章。

## 2026-08-21 AI 跟你对话像真人在打字了:逐字往外蹦,不再转圈干等一整段

以前跟 AI 聊「这段怎么改」「这章哪里不对」,只能盯着「编辑正在想…」转圈,憋半天甩你一整段;生成章节蓝图更是个黑盒,点下去只能干等。这一版把「等」改成「看它写」:

* **对话真流式打字机**:选段讨论、整章问答、重写研讨,AI 的回复现在**逐字往外蹦**,像真人在敲键盘;聊完照旧自动收敛出「修改意见 / 改写建议」和档位建议,一键带进改稿。经中转站 / CDN 反代也不再被攒着一次性下发。

* **蓝图逐章生长**:生成章节蓝图时,进度实时显示「已生成 N / 共 M 章」,看着章节一条条冒出来,不再对着一个不透明的批量调用干等。

* **章节标题先定调,还能一键换一整批**:生成蓝图前先选标题风格——**朴素 / 带钩子 / 悬疑冷峻 / 含蓄诗意**,治「AI 起名个个都『惊天逆转!命运终章!』」的老毛病;已经写好的书也能在大纲页**一键为全书重拟一批标题**,old→new 逐条预览、勾选、还能自己改,只换标题、不动剧情,已写正文不会被标失配。

顺带一批「读起来更像人写的」打磨:

* **治「复读」**:模型偶尔会把同一段写两遍——定稿前自动把重复段落删掉(纯规则、零成本,只删几乎逐字重复的长段,有意的呼应靠长度门槛不误伤)。

* **揪「空洞比喻」**:去 AI 味新增一条重罚规则,专抓「大脑里的涟漪还在」「仿佛有人在很远的地方呼唤他的名字」「无形的手」这类没有具体落点的抽象空镜。

* **补「环境不穿帮」**:章末交接多记一条环境氛围(天气 / 光线 / 声音),防止上一章「四下寂静、没有一只鸟雀」、下一章却「被鸟叫吵醒」这种环境凭空翻转。

## 2026-08-20 章节名称当场就能改:嫌 AI 起的标题太夸张?一键换个朴素的

AI 起的章节名总是「惊天逆转!命运的终极审判」那股味儿,想改还得翻回大纲页,改完正文又被误判「大纲已变、建议重写」?这一版把改名搬到写作页,顺手也把那个坑填了:

* **写作页章首一键改名**:每章标题旁多了「改名」——自己敲一个,或点「让 AI 换个不夸张的」,AI 就着本章大纲给几个克制、贴题、不标题党的候选,点一下即用,不满意还能「换一批」;已写、没写的章都能改。

* **改标题不再误伤正文**:以前光改个章节名,也会把写好的正文标成「大纲已变、建议重写」,还白跑一次 AI 判断。现在认清标题只是「门牌」——改名只改名,正文原封不动,也不再触发下游章节的连锁提醒。

## 2026-08-20 灵感点重构:先给这本书定个「味道」,选了题材就别想跑偏

选了「青春校园」想写细腻的现实初恋,AI 却甩给你「学生觉醒异能」?这一版从根上治「跑题」——生成前先让你给整本书定一个**创作坐标**,AI 全程盯着它写,越线的套路自动拦下重写:

* **创作坐标(本书基因)**:开写前先定味道——参照系、题材模式(现实向 / 幻想向 / 混合向)、五条味道轴(节奏·基调·笔触·结局·尺度)、必须有 / 绝不能有的元素。没头绪?内置「青春现实、甜宠、深虐、热血、悬疑、治愈」等味道锚,点一下一步定好坐标,再随手微调。

* **越线自动毙 + 重生(硬门)**:选了现实向,AI 若冒出重生 / 系统 / 金手指 / 异能觉醒这类套路,会被自动拦下并强化重写、补齐到你要的数量——你只看到干净、对味的方案,看不到跑偏的版本。选题材时就实时告诉你「会拦哪些套路」。

* **品味镜:出方案前先照镜子**:AI 把你定的坐标复述成一段人话——「这本书要写的是……」,还会挑出坐标里自相矛盾的地方(比如同一个元素既「必须有」又进了「绝不能有」)。你确认「对,就是这个味」再出方案,省得烧完 token 才发现不对路。

* **三条路全程对味**:让 AI 出方案 / 改一处 / 边聊边捏,现在都带着这份坐标生成,越捏越贴近你心里那本书。

(内置味道锚均为自撰示意、非任何原作节选。)

## 2026-08-20 渠道体检：套 CDN 的中转站提前亮黄牌，“测试成功却老失败”有解了

“测试连接成功、正式生成却老报错”？实测根因：套了 Cloudflare 的中转站在国内网络下常见**分钟级间歇断连**——「测试连接」只测点击那几秒，而生成一章要连续调用十几次模型、战线几十分钟，几乎必撞断连窗口。本版把这个坑前置到配置时刻：

* **CDN 渠道亮黄牌**：模型设置自动识别套了 Cloudflare 的渠道，配置卡片常显提醒条，保存与测试时同步提示——国内直连这类渠道，容易出现“单次测试通过、长任务中途断掉”。

* **测试连接升级“连测三次”**：对 CDN 渠道，测试通过后再隔 2 秒快测两次，链路正在抖动会直接报“稳定性探测：N 次失败”，不再给单次成功虚假的安全感；非 CDN 渠道测试行为完全不变。

* **报错不再一片空白**：此前“上游连续 3 次调用失败，最后错误：”后面经常是空白（Windows 下这类网络异常天生没有消息），现在会翻译成“网络连接失败/被重置”等可读中文，并告诉你该不该换渠道。

另：主题曲物料升级为**双轨**——新增一整段中文「音乐描述」，快乐虾米、海绵音乐等描述式工具直接整段粘贴；Suno / Udio 标签式工具继续用英文风格标签 + 歌词，一份物料两类工具通用。（本版安装包同时首次带上上一条“去 AI 味再进化”。）

## 2026-08-19 去 AI 味再进化：给笔法找个“老师”照着学，定稿前自动过一道“人话门禁”

这一版把“一眼读得出是 AI 写的”当头号敌人来打，从“只提醒”升级为“锚定 + 门禁 + 自愈”：

* **文风范本（正向锚）**：给 AI 一个具体的“人”去学笔法——内置余华、鲁迅、汪曾祺、金庸、王小波、海明威等文风任选，也可以喂进你自己的范文、或让它从你已认可的章节里自动提取文风。续写、片段润色、整章润色每个入口都吃这个锚。（名家文风均标注“风格参考·非原作节选”，范本为自撰仿写，不含任何原作节选）

* **套话反例（负向锚）**：把常见 AI 腔（神态套话、升华点题、论文式罗列、比喻堆砌）逐条摆成“✗ 这样写 → ✓ 改成这样”的反例，让 AI 照着划掉。

* **定稿“人话门禁” + 自愈**：每章定稿前自动过一道 AI 味打分（8 类套话规则 + 句长节奏 / 段落结构统计），超标就自动定向去味重写、复测收敛；万一没改好或改跑偏会自动回退，绝不给你落一个更差的版本。

顺带一批打磨：选中正文任意一段就地润色 / 描写 / 扩写 / 脑暴、章尾灰字续写一段（Tab 采纳）、正文里人物名高亮可看设定、Ctrl+M 打开“故事地图”纵览全书脉络与伏笔；模型返回异常数据时提示更准、不再连累整章。

## 2026-08-18 稳定性打磨：并发更稳、异常不再带崩整章、提示更准

这一版没有新功能，专注把底层做扎实——多任务并行时更稳，遇到模型抽风也不再连累整章：

* **大纲级联重生成更稳**：改一章大纲触发下游级联重写时，不再偶发"数据库被锁住"导致失败。

* **超长章自动拆章不再带崩**：模型返回异常数据时，拆章会安全回退、保留原文，不再把已生成、已审校的整章拖崩。

* **一致性抽取失败不丢数据**：章后写回故事圣经若中途失败，本章原有的事实不再被清空——再次抽取或重写时数据完好。

* **打分 / 伏笔更抗抽风**：主审吐"优秀""8分"这类非数字不再打穿审校流程；伏笔匹配遇到歧义时宁可不认，也不会错挂到别的伏笔上。

* **重复点击不再起多个任务**：回填节拍、生成文风备忘等操作重复点击会被去重，不再并发起多个任务抢写数据。

* **新建小说不再冒出空项目**：修复某些情况下打开「新建」会静默建出多个空草稿项目的问题。

* **错误提示更干净准确**：后台任务失败时的英文 HTTP 报错统一翻译成中文（登录失效 / 超时 / 连接失败等），提示文字也去掉了冗余前缀。

* **长篇更省 token**：超长篇的上下文注入加了上限（关键设定永不砍），控制提示词随篇幅膨胀。

## 2026-08-17 去 AI 味动真格:生成端全链路压制"刻意腔"

* **草稿 prompt 重写**:删掉"每个场景必须堆两处感官细节"这类硬性指标(它正是"写得刻意"的根源),改为克制导向——细节只落关键瞬间、过渡段允许平淡、对话说人话、节奏有快有慢。

* **反 AI 腔规则补全**:生成端从 4 条粗规扩到与润色端同级的一整套(神态套话、稳妥套话、升华点题、hedge 词、比喻限量、造句定式、句式节奏),标明违反即返工。

* **定稿先去味再定稿**:草稿写完先过一遍 AI 味规则检测,命中的句子逐条列给定稿环节定点改写,不再只靠模型自觉。

* **文风备忘不再学坏**:随书累积的文风备忘明确不沉淀"抒情堆料"这类 AI 腔,防止前几章的浓腔被当成"本书风格"越写越重。

* **结果卡给入口**:生成完成时 AI 味指数偏高的章节会标黄提醒,一键直达本章去味润色。

## 2026-08-17 交互彻底重构:以「章」为中心,手机电脑各自顺手

* **导航大改**:顶部八个步骤合并成三个区——「开书」(概念/架构/大纲,向导感保留)、「写作」(日常都在这里)、「全书」(看板/投稿/体检/翻新)。旧链接自动跳转,不会 404。

* **当前章跟着你走**:正在看的章号记进了网址——写作、评分、校对、润色之间切换不用反复选同一章;刷新不丢、网址可收藏可分享;看板点章节格子直达该章。

* **写作页三栏**:左边章节轨(唯一选章入口)、中间正文、右边参考抽屉(蓝图/人物/伏笔/审核报告/历史版本,平时收成一条图标);底部动作条:生成/重写/润色/校对/评分/连写。评分建议点「按此重写」直接就地填好意见框,不再跳页面。

* **设置搬家**:字数守卫、审校把关、连写要求、世界观硬规则统一收进右上角 ⚙︎ 设置,不再常驻写作页侧栏。

* **手机端重做**:进来就是正文;底部栏(写/读/参考/设置);右下角悬浮按钮点按生成本章、长按展开更多动作;正文左右滑直接切章;参考和动作面板全屏弹出。

* **桌面端加成**:快捷键全套(Ctrl+S 保存、Ctrl+Enter 生成本章、Ctrl+\[ / ] 上下章、F11 沉浸写作),Ctrl+K 命令面板输入章号或动作名直达;桌面客户端新增系统菜单栏、「对照阅读窗」(另开一个只读窗口对照旧章,主窗保存后它自动刷新)、窗口大小位置记忆。

## 2026-08-06 世界观硬规则钉板:设定常识钉一次,全书生成不违背,还能全书扫错

* **钉板**:审核报告页新增「世界观硬规则」——把本书不可违背的设定/常识钉死(如"2024 新高考,理科生不考政治""高考只考 6.7-6.8 两天"),每行一条。保存后注入蓝图、草稿、定稿、修改指令等所有后续生成,模型写到相关设定必须照此执行,从根上减少全文性事实错误。

* **规则扫描**:一键逐章体检已成文正文,把违反钉板规则的章节全部扫出来——错得前后一致、全书体检抓不到的问题(如理科生背政治)也能定位;每条违反带原文证据和修正建议,落进各章「审核报告」,按建议修订/重写链路逐章修复。

## 2026-08-06 模型设置大升级:多配置一键切换,长章节不再被中转站掐断

* **修复长生成被掐断**:章节生成耗时长,套了 Cloudflare 的中转站会在约 100 秒处掐断请求(HTTP 524,报"响应非 JSON"HTML 错误页)。现在遇到这类瞬时错误(524/429/5xx/超时)会自动退避重试,并改走流式聚合——边生成边收,CDN 不再掐;错误提示也改为准确说明"生成耗时过长被 CDN 掐断/渠道拥挤",不再误导你检查 Base URL。

* **多配置管理(cc-switch 风格)**:模型设置从"固定三家卡片"升级为**多套命名配置**——可同时存「DeepSeek 官方」「中转站 A」「中转站 B」等任意多套,每套独立名称/key/地址/模型,一键「设为默认」「设为快档」切换,支持测试连接与安全删除(连通配置需二次确认)。存量配置自动迁移,无感升级。

* **双档路由真正生效**:架构/定稿/事实抽取走「默认」配置(强模型),草稿/摘要/校验走「快档」配置(便宜快模型);未设快档时跟随默认。草稿/定稿输出预算提升至 16384 tokens,长章节不再被截断。

* 每套配置可单独覆盖超时与 max\_tokens(0 = 跟随全局);老配置启动时自动迁移,API key 全程密文存储。

## 2026-08-02 修复:改过大纲的章,重写不再被旧正文带偏

* 大纲改过的章(标「大纲已变」)点重写时,不再把上一版正文喂给 AI 做参照——旧正文基于旧大纲,会把新情节拉回老路。现在改为明确指令"完全按新大纲重新构思",你的补充意见仍可填写、可留空。

* 失配章的重写框新增提示,说明这次会按新大纲重新构思;大纲没改过的章重写行为不变。

## 2026-08-01 大纲也能聊了:单章大纲 AI 研讨

* **单章大纲 AI 研讨**:大纲页展开任意一章,点「AI 研讨」即可就多轮对话聊清"这章大纲哪里不对"——AI 带着架构简报、本章大纲、相邻章摘要和成文状态来聊,不再是大纲出错只能手改或重生成。

* **改写提案卡**:聊到方向明确,AI 自动蒸馏出改写提案(新标题对比、新简述、改动说明);点「应用这份改写」走修改指令的 apply 链路落库——版本化记录、已有正文的章自动标失配提醒重写;也可以「先不应用继续聊」。

## 2026-08-01 审核闭环补全:全书体检 + 剧情时间线 + 审核报告常驻

* **审核报告常驻**:每章正文旁新增「审核报告」按钮,随时查看该章门禁状态、可操作问题清单(按建议修订/人工解决/忽略)与最近一次主审评分——不再只在刚生成完那一屏出现。

* **全书体检**:编辑部 → 审核报告新增「全书体检」,LLM 逐章对照圣经/上章契约/上章结尾,把跨章矛盾一次扫出来,按「诊断」来源落进各章审核报告逐条处理。

* **契约工具**:契约提错了可在审核报告一键「重新提取契约」(重提上章+本章契约并重检门禁);老书缺契约可在编辑部「批量补提契约」补齐,审核报告直接显示缺几章。

* **剧情时间线**:看板概览新增剧情时间线(各章章末时间/地点,从契约聚合);同时注入写前预审与写后门禁,跨章"时间倒流"类矛盾也能抓了。

* **翻新「修改要求」**:好几章都有的文字层共性问题(如"每章结尾都总结点题"),写在翻新面板的修改要求里,轻度重润逐章注入(仍不改剧情)、重度重写作为重写意见。

* **文风备忘可手改**:翻新面板可查看/手动编辑文风备忘,改后自动累积在你的版本上继续;清空则重新自动累积。

* 章节地图补全「待审/已审/拦截」中文状态与图例,不再显示英文。

## 2026-08-01 章节审核体系上线:前后夹击,矛盾章不再混进书

* **章末交接契约**:每章定稿自动提取结构化"章末状态"(剧情时间/地点/人物即时状态/未了意图),写下一章时注入——"上章深夜刚睡,下章开头发呆"这类矛盾从根上少了。

* **写前预审**:动笔前比对本章蓝图与上章契约,矛盾随生成结果"写前提醒"给出(如蓝图写清晨出发、上章深夜刚入睡)。

* **写后门禁**:一致性检查升级为三路对照(圣经+上章契约+上章结尾),每条矛盾带严重度/原文证据/修正建议;**致命矛盾(blocker)自动回炉修订**,封顶仍不解决则章节被**隔离**——不进圣经、不进摘要、连写队列暂停,矛盾不再固化进书。

* **人工审核**:干净章落库为「待审」,点「通过」转「已审」;被拦截章可「放行(忽略全部)」或重写;每个矛盾可「按建议修订/已人工解决/忽略」。

* **审校第五维**:连续性计入达标判定;设置新增「连写要求上一章审核通过」开关(默认关,仅拦截才暂停)。

* 存量章节自动迁移为「已审」,老书无感;契据提取失败留痕可查,不静默。

## 2026-08-01 细节打磨:更新进度不跳变 + 向导更顺了

* 更新下载不再"50% 跳到 0%":连接等待阶段改为往返扫描动画,拿到真实进度后才按百分比推进。

* 新建向导题材屏减负:AI 推断出题材就自动采用并进入下一步,不用停下来再选一次;推断失败才停下来手选,确认墙随时可回改。

* 修复锁屏页 jW 字标的 W 换行溢出徽章的问题。

* 修复右侧档案栏「一句话故事/核心钩子」被挤成一字一行的排版,改为纵向排列、多行省略。

* 修复窗口不全屏时步骤条看不到「确认/点火」:窄窗自动收紧间距并隐藏缩略卡,当前步自动滚入视野。

## 2026-08-01 桌面端体验大修:锁屏焕新 + 托盘 + 16 项交互修复

* 应用锁锁屏全新视觉:品牌光晕 + 玻璃拟态卡片,明暗双主题;顶栏新增「锁定」,不用重启 app 即可锁屏。

* **关闭守卫**:有生成/翻新任务时点关闭会弹确认(直接关闭 / 最小化到托盘 / 取消);新增系统托盘,点图标恢复窗口;「偏好」可开"关闭窗口时最小化到托盘"。

* **单实例**:重复启动聚焦已有窗口,不再双开后端抢数据库。

* 后端慢启动/崩溃不再误显登录页,改为"连接失败 + 重试"页。

* 新建向导:回改题材/灵感后,概念与书名候选会提示"基于之前的题材",可一键重新生成或仍用这批。

* 交互修复一批:覆盖全部蓝图/放弃创建/删除 provider/覆盖偏好档案前都有确认;正文手改后切章提示未保存;危险确认焦点移到「取消」,回车不再误删;阅读器误点空白改为分层收起,不再丢选段和对话;LLM 报错中文化(401/429/超时等不再甩英文原文);项目状态徽标中文;指引统一指向「编辑部」;「退出」「忘记密码?」等可键盘操作;重写前提示旧版自动存快照可回退。

## 2026-08-01 桌面版:更新可以走代理了

* 修复「下载更新」永远直连 GitHub 导致代理环境下失败(error sending request for url)的问题:更新器的网络库不读系统代理,此前无论你代理开没开都直连;且镜像只对"检查"兜底,安装包地址写死在 GitHub,所以会出现"检查得到、下载失败"。

* 设置页「关于 & 更新」新增**更新代理**:填 `http://127.0.0.1:7890`(支持 http/socks5)后,检查与下载都走它;留空为直连;填错会自动回退直连再试一次,不会永远更新不了。

* 更新包仍由内置公钥验签,代理只是传输通道,无法伪造内容。

* 注意:本版需从 Releases 页手动下载安装一次(浏览器可走代理),之后版本即可在应用内正常更新。

## 2026-08-01 新建向导焕然一新 + 账号安全补齐

* 新建小说向导升级为**三幕式沉浸流程**:一屏一焦点(灵感 → 概念 → 题材 → 倾向 → 书名 → 篇幅),AI 候选以抽卡形式呈现,可"选用 / 换一批 / 带反馈重新生成 / 自己写",确认瞬间卡片飞入顶部进度条;全程动效衔接 + "AI 构思中"微文案。

* 新增**确认墙**:开写前总览全部设定,可跳回任意一步修改,下游项自动标注"可能受影响"。

* 点火升级为**流水线**:架构 → 蓝图双步骤卡实时展示进度,失败只重跑该步;中途退出可"继续创建"接着来。

* 设置页新增**修改密码**(需登录的部署):验证旧密码,两次输入即时校验。

* 桌面版新增**应用锁**:设置密码后每次启动需输入才能进入;忘记密码可在锁屏页点「忘记密码?」重置(书籍数据不受影响)。

* 部署运维:新增脱机重置密码脚本 `python -m scripts.reset_password --user admin`,旧密码丢失/过于复杂无法输入时可从服务器 shell 直接重置。

## 2026-07-29 桌面版:检查更新不怕网络抖动了

* 修复「检查更新」偶发报"error sending request for url"的问题:国内直连 GitHub 不稳定,检查请求现在失败会自动重试(最多 3 次),并加了镜像兜底通道,GitHub 直连不通时自动走镜像。

* 更新包仍由内置公钥验签,镜像只作传输通道,无法伪造内容。

## 2026-07-29 桌面版:修复「检查更新」报错 + 安装界面换新

* 修复设置页「关于 & 更新」点检查更新报"Command check\_update not allowed by ACL"的问题——桌面壳自定义命令未向系统权限层声明,现已补全授权,检查更新/后台下载/一键重启恢复正常。

* 安装界面品牌化:欢迎页与页眉换上 jW 品牌图,安装器文案改为简体中文,不再是原生英文裸界面。

## 2026-07-28 已有书「翻新」上线 + 生成质感全面提升

* 新增**重构翻新**(工作台第 6 步「翻新」):为已有章节回填场景节拍、扫描正文生成文风备忘;轻度重润锁情节去 AI 味、不改剧情;重度重写带节拍与文风备忘整章重跑,自动重抽圣经、重建摘要。

* 章节蓝图新增**场景节拍**:每章 3-5 个场景节拍,正文按节拍铺场景,不再一句简述撑几千字;旧书可用「回填节拍」补齐。

* 新增**文风备忘**:每写完一章自动累积"这本书怎么写"(调性/人物声音/复现意象),注入后续章节,长篇后段人物声音不再漂移。

* 正文质感:草稿/定稿提示词重写,强化视角纵深、感官细节、人物声音区分与潜台词;不同任务使用不同采样温度(创作发散、抽取求准)。

* 看板升级:章节地图新增主审四维分与 AI 味指数,一眼看出哪章需要返工。

* 翻新任务中途失败不再白跑:已完成的章自动保留,面板提供「续跑剩余章节 / 重试失败章节」一键补救;文风备忘提示区分已存在与生成失败。

* 修复中转站"HTTP 200 但夹带错误"被当成正常响应的问题(如"无可用渠道"现在会明确报错,不再退化成空回复)。

## 2026-07-25 封面提示词更细致 + 主题曲风格看得懂了

* 封面提示词大幅写细:按「人物 → 场景 → 氛围 → 光影 → 构图 → 画风」六层展开,每套方案含中文提示词(即梦)、英文提示词(Midjourney)与负面词,复制即可出图。旧方案点「重新生成」即可换新。

* 主题曲新增**风格中文对照**:英文风格标签逐条翻译,并附"在其它软件选哪类曲风"的建议——用 Suno 粘英文标签,用妙音、Mureka 等照着中文对照选。

* 修复移动端顶栏「任务」抽屉内容被截断看不全的问题:阶段与错误信息现在完整换行显示,点空白处即可收起。

## 2026-07-25 全局深色模式 + 桌面版更新更稳了

* 新增**深色模式**:设置页「偏好 → 外观」可选跟随系统 / 浅色 / 深色;全局暗色下阅读器默认切换为夜间主题(仍可在阅读器内单独调)。

* 全站视觉统一打磨:标签、徽章、空状态等此前多套并行样式归一;弱化文字对比度提升,长时间阅读更省力。

* 可访问性:章节导航、倾向标签、卡片等全部可点元素支持键盘操作(Tab 聚焦 + 回车触发)。

* 修复桌面版**更新可能装不上/装一半**的问题:安装更新前会先停掉本地后台服务,避开 Windows 文件占用;重启应用时后台服务也不再残留旧进程。

* 修复桌面版后台服务启动卡死时"双击图标没反应"的问题:启动有超时,失败会弹出明确的错误提示。

* 安全加固:桌面版本地服务增加来源校验(防恶意网页借 DNS 重绑定操控);API Key 加密密钥改为每台机器独立随机生成(老数据自动迁移,无感)。

* 设置页「偏好」全端可见(外观选择网页版同样生效),自动检查更新开关仍仅桌面版显示。

## 2026-07-25 全新「设置」页 + 更漂亮的桌面版更新体验

* 新增独立「设置」页(顶栏「设置」进入):一处集齐「关于 & 更新」「模型设置」「偏好」。

* 桌面版能看到当前版本号了,也能主动点「检查更新」,不必再干等自动提示。

* 发现新版本后改为**后台静默下载**,下好再提示「重启生效」,一键重启完成升级——不再打断你写作。

* 更新界面从系统原生弹窗换成应用内自绘 UI,和工作台同一套视觉,带下载进度条,更好看。

* 更新全链路加了日志(应用日志目录 updater.log),万一"更新不生效"可据此排查,不再是黑盒。

* 「模型设置」从旧的独立页并入设置页,DeepSeek / OpenAI / Gemini 增删改测体验统一。

## 2026-07-25 桌面版一批"点了没反应"的问题修好了

* 修复新建小说点"我知道想写什么类型"后流派卡片墙一片空白——打包时漏带了题材库配置文件,现已打入安装包。

* 修复编辑部"优化动作"在桌面版加载不出来的同类问题。

* 修复桌面版"导出 Word"会失败的问题——打包时漏带了 Word 模板文件。

* 顶栏去掉了"API"入口(那是给开发者看的接口文档,对写作没用);/docs 地址本身仍保留,开发者随时可访问。

## 2026-07-25 桌面版应用内自动更新,不用再手动下载安装包

* 桌面版启动后会自动检查新版本,发现更新即弹窗提示「立即更新 / 稍后」,点一下就在后台下载、安装并自动重启,全程不用打开网站找安装包。

* 更新包带签名校验,只认官方发布的版本,安全可靠。

* 注意:这是最后一个需要手动下载安装的版本——装上它之后,往后的更新都能在应用里自动完成。

## 2026-07-25 桌面版顶栏按钮修好了,另加「首页」入口

* 修复桌面版（Windows 安装包）里顶栏「模型设置」「API」「GitHub」点了没反应的问题——此前这些链接按浏览器新标签页的方式打开，而桌面窗口不处理这种请求。

* 现在桌面版里：「模型设置」直接在应用窗口内打开（配完 key 点页面里的「返回工作台」即回）；「API」「GitHub」改用系统默认浏览器打开。网页版行为不变。

* 顶栏新增显眼的「首页」入口，随时一键回到作品列表。

## 2026-07-24 老书不用手填,档案从内容里自动提炼

* 已经写出内容的书,打开架构页会自动从概念/架构/简介/抽样正文反向提炼出创作偏好档案,直接启用,并弹提示让你知道——不用自己再填一遍。

* 因为是从你实际写出的文字归纳的,档案天然和正文一致,不会填进去对不上。

* 档案卡新增「从已有内容提炼」按钮,大改之后可随时重新归纳;提炼结果随时可手动微调。

## 2026-07-24 创作偏好档案,把主张沉淀成全书的"宪法"

* 架构页新增「创作偏好档案」:文风 / 禁忌 / 读者定位 / 其他主张四个字段,填一次,生成、重写、润色、大纲、架构每个环节都高优先级遵循——不用每次重复交代。

* 和 AI 研讨(架构研讨 / 重写研讨)聊出的主张,点「存进偏好档案」一键吸收,AI 会自动归类合并进对应字段,对话不再用完即弃。

* 档案随项目长期保存,正文怎么改都不影响它;它管的是"这本书该怎么写",不是某一章。

## 2026-07-24 校对结果也回显,打开即见

* 生成时自动修掉的硬伤会逐条留存,打开「编辑部 → 校对」直接看到修了哪些(原文 → 改后),不必再点一遍「开始校对」。

* 手动校对的结果同样留存:下次打开自动回填待修清单,勾一勾就能修。

* 与主审回显一致:正文一旦改动,旧校对清单自动失效,不会拿过期结果误导你。

## 2026-07-24 重写前先和 AI 聊聊,改得更准

* 重写章节时新增可选对话:说不清哪里不满意,就先和 AI 编辑聊几轮,它会把你的想法蒸馏成一条精准的修改意见,填进重写框再重写。

* 原来的快速重写照常保留:直接填意见或点优化方向 chip,一键重写,互不耽误。

## 2026-07-24 主审结果回显,不用重复审

* 生成时和手动审校的主审结果都会留存,打开「编辑部」直接回显最近一次评分,不必再点一遍「请主编审读」。

* 回显会标注来源(生成时审校 / 手动审校)、时间、回炉轮数与自动修复的硬伤数。

* 正文一旦改动(编辑 / 润色 / 重写 / 回滚),旧评分自动失效,避免对着新正文看旧分数。

## 2026-07-23 生成时审校把关 + 版本更新提醒

* 生成章节时自动跑「编辑部」流程:先校对、硬伤自动修掉,再请主审四维打分。

* 不达标会带着主审意见自动回炉重写,有上限(默认 3 轮),到点接受最好的一版——告别生成后手动「一直回炉」。

* 写作页新增审校配置:达标线(默认四维均 ≥7,可调到 8/9)、回炉上限、自动回炉开关。

* 生成结果卡与编辑部都会显示「达标 / 未达标」状态。

* 新增版本更新提醒:发现新版本时顶部提示刷新,并可查看本次更新内容。

