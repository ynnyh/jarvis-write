# 19 核心梗为纲:梗卡 / 作战图 / 追溯与对账(规划与实施记录)

> 2026-09-12 立项。源头是作者的原始诉求:「写第一章时,怎么方便地看到这章的章纲、
> 伏笔、出场人物、结尾钩子,以及这些角色、情节、钩子背后的关联关系,能不能随时
> 点进去追溯;再有个简单场景地图理顺情节进展;不管我怎么安排实际剧情,这些关系
> 都自动帮我建联、纠正——这就是不崩、能改、可控。」
> 后经讨论确立:**小说的核心是核心梗**——梗是入口,兑现循环是留存,边界是生命线;
> 整个系统应以核心梗卡为中心对象组织交互。

## 0. 四条铁律(全案不变)

1. **梗是纲**:梗卡是一等对象;蓝图逐章标「梗兑现」,作战图/对账/体检都是它的投影。
2. **圣经是唯一真相**:图谱/地图/档案是确定性投影,AI 抽取只做补充且必须带证据锚;
   绝不让可视化层变成第二份要维护的状态(两份真相必然漂移)。
3. **点点点**:任何对象两跳内回到原文证据(章/段落锚)。
4. **AI 建联给作者裁决权**:抽取产物可一键否决;作者手写的永远权威。

## 1. 里程碑与状态

| 里程碑 | 内容 | 状态 |
|---|---|---|
| M1 核心梗卡 + 本章作战图 | premises 表(高概念/兑现机制/节拍表/边界禁忌/钩子计划);开书向导确认步 AI 提炼预填;本书设置编辑 + 全书补标节拍 job;蓝图逐章「梗兑现」字段并注入正文;作战图(梗行/章纲/出场人物带本章有效关系/伏笔账/场景条/承上钩子) | ✅ v0.42 候选(5f27b76) |
| M2 追溯层 | relationships.evidence_fact_id 证据锚(边→同章双写 fact 直达);EntityCard 浮卡补关系区(对方·关系·自第N章 + 证据章引用)与出场章跳章 | ✅ (b24c525) |
| M3 交稿对账 | 抽取顺带梗兑现记账(premise_ledger,同章唯一);抽取新关系边 status=pending(不拦截注入,对账区裁决);对账端点(GET/confirm)+ 结果卡「本章对账」区(确认/否决/全部确认) | ✅ 本批 |
| M4 梗健康度 + 关系图谱 | 体检「梗健康度」(兑现占比/最近与最长连续未兑现/无对账账的已写章/文扑预警);关系图谱页(手写 SVG 圆形布局 + 章节时序滑块) | ✅ 0afc321 |
| M5 情节推进图 | 章×场景网格(张力色阶/情绪/地点),伏笔埋收带从埋设横到回收/预期(逾期标红);GET /plot-map 纯投影 | ✅ 0afc321 |

## 二轮评审与后续批次(2026-09-13/14)

- **连接级独立超时**(v0.43.1,a3f4878):渠道端口不可达时 httpx 总超时黑等 600s,
  三个适配器统一 connect 10s 快速失败——深水区评审实测抓到。
- **口味定标 + 分叉口味回收**(未发版):引擎卡 prompt 把用户勾选口味定为红线;
  suggest-shape 标签过滤从等值改包含匹配(「悬疑烧脑」→悬疑),不再全过滤空;
  首页口味面板修为全路径可见(原先只挂在方向卡路径)。
- **结构化人物画像**(未发版):persona.py 架构提炼后自动立 5 张画像卡落
  Entity.base_profile.persona(author 来源重提炼不覆盖);生成注入 personas_block、
  门禁比对言行崩人设;人物看板结构化编辑 + AI 立绘提示词。
- **引擎锚点重抽**(未发版):EnginesRequest.anchor_engine,「🎯 照这张再来点」
  沿认可内核出变体,与 avoid 全盘否定互补。
- **重写检测回写**(未发版):GateResolve/GenResultCard 重写完成后回传新检测结果
  替换旧快照,修掉「点了重写但徽标还挂旧问题」。
- **连写停止原因可见**(v0.44.0):恢复条带停止原因并持久化,切走再回来还在。
- **epub 元数据**(v0.44.0):dc:creator/dc:description/dcterms:modified + 书名页。
- **首页驾驶舱 + 新手轨三步清单 + 制片页签分期 reveal**(v0.45.0,b568dd4)。

## 远期谋划(记录不排期,同正文)

## 2. 远期谋划(只记录,不排期)

- **世界模型接口**:`WorldProvider` 抽象;现阶段以确定性规则模拟过渡(地理距离/
  行程耗时/势力约束的空间一致性检查),世界模型商用后替换为真模型。不进关键路径。
- **世界资产包**(三国仅是示例方向):历史/架空世界的地理(州郡-关隘-城池)/势力/
  人物(生卒、阵营流转、关系)/事件年表的结构化资产,内置「历史严格模式」与
  「架空 fork 模式」;依赖 M4/M5 的图谱与地图先行验证工作流。
- **地图化创作沙盒**:作者以角色视角在世界里走 → 触发事件 → 一键「写成第 N 章场景」
  → 回到场景级生成管线。

## 3. 关键设计决策记录

- **梗卡三件套**:高概念(入口)/ 兑现机制(留存)/ 边界禁忌(生命线)。
  光有梗撑不过三十万字;系统要建模的是「梗的可重复兑现」。
- **pending 不拦截注入**:抽取新边 status=pending 只是对账 UI 状态——边本就从真实
  正文抽出,历史行为就是直接进圣经;拦截会静默撤掉安全网。否决 = 删边 + 收口其
  证据事实(本章新立直删,更早的 valid_until=n-1,KnowledgeState 引用先清)。
- **存量为 confirmed**:迁移不翻 pending,否则存量书的关系约束被静默撤掉。
- **作战图是投影**:零新抽取,聚合 Outline/Scene/Premise/Foreshadowing/Entity/
  Relationship/ChapterState.contract;缺数据如实缺省,不填 0 冒充没问题。
- **实体浮卡先行**:Prose 实体高亮 + hover 卡 + CharacterCard.appearance_chapters/
  RelationEvidence 在 v0.40 前后已落地,M2 只补关系区、证据展示与跳章。

## 4. 验证基线

- 后端 pytest 全量(含梗卡 round-trip / 作战图聚合与时序关系 / 证据锚 /
  对账确认-否决-收口 用例);前端 vitest(作战图 / 梗卡 / EntityCard / 对账块)。
- 手机 390 + 桌面 1280 双视口走查,横向溢出探针 = 0。
