// 漫剧工坊引导:进来先看这张卡就知道该点哪儿。
// 两块分开收起(各自的 localStorage 键):
// - DramaGuide:这是什么 + 五步怎么走 + 按钮点不动怎么办(顶部);
// - DramaProductionGuide:拿到手册之后在本站之外怎么出片(底部,最容易迷路的一段)。
// 样式沿用 ProjectPage 引导条那套(notice/step-guide/guide-body/guide-mini),不另起风格。
import { useState } from "react";

/** 可收起引导卡的公共外壳:收起后只留一枚小按钮,不占版面。 */
function Collapsible({ storageKey, mini, children }: {
  storageKey: string;
  mini: string;
  children: React.ReactNode;
}) {
  const [hidden, setHidden] = useState(() => localStorage.getItem(storageKey) === "1");
  if (hidden) {
    return (
      <button type="button" className="guide-mini muted"
        onClick={() => { localStorage.removeItem(storageKey); setHidden(false); }}>
        ⓘ {mini}
      </button>
    );
  }
  return (
    <div className="notice notice-info step-guide drama-guide">
      <div className="guide-body">{children}</div>
      <div className="guide-side">
        <button className="btn-sm"
          onClick={() => { localStorage.setItem(storageKey, "1"); setHidden(true); }}>
          收起
        </button>
      </div>
    </div>
  );
}

export function DramaGuide() {
  return (
    <Collapsible storageKey="drama-guide-hidden" mini="漫剧工坊怎么用">
      <div className="mb-2">
        <b>这是什么:</b>把已定稿的小说改成一份<b>拍摄手册</b>——分镜表 + 每格的绘图提示词 +
        配音稿 + 剪辑清单 + SRT 字幕。本站<b>不出图、不出视频、不合成语音</b>:那几步在你顺手的
        工具里做(见下方「出片指引」),这里负责把「该拍什么、怎么拍、提示词怎么写」全给你。
      </div>
      <div className="mb-2"><b>照着 ①→⑤ 走</b>(每步都能单独重跑,改一步不用推倒重来):</div>
      <ol className="drama-guide-steps">
        <li>
          <b>风格卡</b>:先拍板画风方向(拿不定就点「AI 荐方向」),AI 写一段「画风锁定段」。
          这段会逐字塞进之后<b>每一格</b>提示词——上百格画风不漂移靠它。
        </li>
        <li>
          <b>角色/场景卡</b>:从故事圣经批量出「锁定外貌段」,同一个人不换脸靠它。
          每张卡的<b>性别</b>单列一栏,AI 判错了你点一下就改,之后定妆照与每格提示词都照它写。
          再点「出定妆照」——每个角色一条参考图提示词,<b>先出一张正面半身定妆照上传回来</b>,
          之后每格出图都把它当参考图传给生图站,人物才真锁得住脸(光靠文字描述压不到零)。
          手改过的卡点「🔒 锁定」,以后重跑不会覆盖你的改动。
        </li>
        <li>
          <b>切集</b>:选已定稿的章节范围,AI 按短剧节奏切成一集集(默认 90 秒一集),
          每集自带开场钩子与结尾卡点。一章可拆多集,过渡章也可以数章并一集。
        </li>
        <li>
          <b>单集流水线</b>:在集列表里<b>点任意一集</b>展开,然后按顺序
          ④-1 写剧本 → ④-2 拆分镜 → ④-3 出提示词 → ④-4 出成片包,最后「导出手册」。
          一格提示词不满意,用那格的「重出这格」补要求重生成,不必整集重跑。
          分镜出来后,最下面的「<b>视频段计划</b>」会按你那家视频站的<b>单次时长上限</b>
          (5/10/15 秒)把格并成段——一段生成一次,再照段号在画布里拼。
          在外面出好的图<b>挂回那一格</b>、做完的格<b>打个勾</b>,这一集的进度(静帧 N/M、
          视频 N/M)就一直记在站里,导出的施工单也带着它——几十格的手工活,靠脑子记必做丢。
        </li>
        <li><b>预告片</b>(可选):从各集的钩子/卡点混剪一条 30-60 秒宣传片,发布引流用。</li>
      </ol>
      <div className="mb-2"><b>不想出站?分镜格里能直接出片</b>(设置 → 出片引擎 填一次 autodl.art
        的令牌,按格点「出片」就行):</div>
      <ol className="drama-guide-steps">
        <li>
          <b>① 配令牌</b>:到「设置 → 出片引擎」粘贴 autodl.art 的令牌(令牌管理页创建,
          分组选 ComfyUI)。费用约 ¥0.02/秒,重 roll 一次几分钱,不满意就连出几版挑。
        </li>
        <li>
          <b>② 出图挂静帧</b>:按老流程拿提示词去生图站出图,挂回那一格——
          有静帧的格出片走<b>首尾帧</b>工作流(静帧当首帧,人物长相稳);
          没静帧直接出片会走<b>文生视频</b>,同一格每版可能换脸,只适合空镜先看个大概。
        </li>
        <li>
          <b>③ 出片与重 roll</b>:每格「让这一格动起来」里有<b>本站直接出片</b>按钮,
          点一下等 1-3 分钟,草片就地预览;不合适点「重出一版」,版本都在下拉里,
          挑中意的点「设为成片」。「视频出好了」的勾仍由你自己打。
        </li>
        <li>
          <b>④ 对白格开口说话(完整档)</b>:先回 ② 角色卡给说话角色<b>传一段音色参考</b>
          (5-10 秒干净人声),分镜格上选好<b>配音情绪</b>,点「出片」= 自动配音 + 对口型。
          没传音色的对白格回退普通出片(不说话),版本列表和任务说明里都会写明原因。
        </li>
      </ol>
      <div><b>按钮点不动 / 报错了:</b></div>
      <ul className="drama-guide-steps">
        <li>「拆分镜」是灰的 → 这一集还没剧本,先点 ④-1 写剧本。</li>
        <li>「出提示词」是灰的 → 还没分镜,先点 ④-2 拆分镜。</li>
        <li>提示「先生成美术风格卡」→ 回 ① 定画风,提示词的画风锚从那里来。</li>
        <li>「出定妆照」提示「还没有美术风格卡」→ 同上,定妆照要带画风锚才跟正片是一套。</li>
        <li>
          <b>女角色被写成男的(或反了)</b> → 那张卡最上面有<b>性别</b>一栏,点「女」拍板
          (点一下就存),再点同一行的「<b>重出这张卡</b>」让 AI 按性别重写外貌/服饰/声线;
          定妆照提示词不会自动跟着变,接着点「重出提示词」。卡上每一栏(含<b>英文外貌关键词</b>)
          都能直接手改,改完点「保存」——只改中文那栏没用,生图站吃的是英文那条。
        </li>
        <li>
          卡上出现「⚠ 描述里…与标定性别不符」→ 提醒而已,不会自动改你的字:
          真是女扮男装就无视它,写错了就改性别或改描述。
        </li>
        <li>
          点「出定妆照」提示「都已经有了」→ 只补缺不覆盖,想重写某个角色用那张卡上的
          「重出提示词」。上传定妆照被拒 → 只收真的 PNG/JPG/WebP(改扩展名没用),
          单张 4MB、一个角色最多 3 张。
        </li>
        <li>
          提示「其中 N 张由引擎拼好」→ 模型那几条没给全(输出被截断是常事),引擎按
          「外貌锚 + 画风锚 + 构图规范」确定性拼了一条,照样能直接拿去出图;想换写法
          点那张卡的「重出提示词」。
        </li>
        <li>提示「没有正文」→ 这一集的源章还没定稿,回写作区把那几章定稿。</li>
        <li>
          <b>生视频时人物换了脸</b> → 八成是把<b>出图</b>提示词粘进视频站了。视频那栏要用
          「让这一格动起来」里的粘贴版:它只写运动与镜头,不写外貌——首帧图已经把长相钉死,
          文字里再描述一遍,模型就会照文字重画。另外确认首帧图<b>真的传上去了</b>(没传 = 文生视频)。
        </li>
        <li>
          <b>视频段计划里某段标了「要生成 N 次」</b> → 这一段比你选的单次上限长:
          按段里的提示用<b>尾帧续接</b>,或回分镜表把那一格拆短。嫌段太碎就把上限调到 15 秒,
          能并的格会自动并成更长的段(同场景、不新增角色、最多一条台词才会并)。
        </li>
        <li>
          <b>某段标了「首帧图还没挂」</b> → 那一段的<b>第一格</b>还没挂静帧。图生视频要先有首帧图,
          所以先拿那一格的提示词出图,再在那一格的「<b>挂静帧</b>」把图挂回来(挂上就自动勾
          「静帧出好了」,段计划里立刻变「首帧图已就位」)。挂在段里第二格没用——首帧一律取段首格。
        </li>
        <li>
          <b>视频文件能不能传上来</b> → 不收,刻意的:一条几十 MB,一集就吃满整个项目的上传配额,
          而剪辑本来就在你本机做。那一格的「<b>成片在哪</b>」填个文件名/目录就够了,
          它会一起写进导出的逐格施工单,剪辑时照着对号。
        </li>
        <li>
          点「复制」后弹出「手动复制」小窗 → 你的浏览器不允许页面写剪贴板
          (多见于用 <code>http://</code> 地址打开本站)。窗里的文本已经替你全选,
          按 Ctrl+C(Mac ⌘+C)即可,也可以点「再试一次」。
        </li>
        <li>整个工坊进不去 → 一章都没定稿。漫剧改编的是小说<b>成稿</b>,先去写作区定稿几章。</li>
      </ul>
    </Collapsible>
  );
}

export function DramaProductionGuide() {
  return (
    <Collapsible storageKey="drama-outguide-hidden" mini="出片指引(手册拿到之后怎么做)">
      <div className="mb-2">
        <b>出片指引:</b>下面这几步在<b>本站之外</b>做。导出的手册里每一格该干什么都写好了,
        照着顺序走一集大约十几分钟。
      </div>
      <ol className="drama-guide-steps">
        <li>
          <b>先出定妆照</b>:回 ② 角色卡,每个角色的「一键粘贴」整段粘进生图站出一张正面半身像,
          满意了下载,再传回角色卡存好。这一步花几分钟,省掉后面上百格换脸的返工。
        </li>
        <li>
          <b>出图</b>:每格提示词那里有「一键粘贴 · 你用的生图站」下拉——
          <b>只有一个描述框</b>的站(GPT-image / DALL·E / 豆包 / 通义)选第一项,负面词已经改写成
          「不要出现」并进正文,<b>整段粘一次就行</b>;<b>有负面词框</b>的站(即梦/可灵/SD)选第二项,
          正文与负面词分开粘;Midjourney 选第三项(英文 + <code>--ar</code>/<code>--no</code> 已带好)。
          出图前先在站点点「上传参考图」把该角色的定妆照传上去。同一集一次做完,别换平台换模型,
          换了画风就漂。出好的那张<b>挂回那一格</b>(每格提示词下面的「挂静帧」)——它就是下一步
          图生视频的<b>首帧图</b>,挂上了段计划才会亮「首帧图已就位」,也顺手把进度记住了。
        </li>
        <li>
          <b>让它动起来</b>(重点,最容易做错的一步):视频站<b>单次只能出 5-15 秒</b>,而分镜格是
          2-8 秒,所以正确做法是「<b>一段生成一次,再在画布/剪映里首尾相接</b>」。集详情最下面的
          「<b>视频段计划</b>」已经替你并好段了:先按你那家站的上限点 5/10/15 秒,表里每一段写清
          <b>首帧用哪一格的静帧、这一段怎么动、几秒、要生成几次</b>,一段一段照着「一键粘贴 ·
          你用的视频站」走。
          <ul className="drama-guide-steps">
            <li>
              <b>先传首帧图再粘提示词</b>:人物长相全靠首帧锁住,所以视频提示词里
              <b>刻意不写外貌/服饰/画风</b>——写了模型会照文字把脸重画一遍,这是图生视频翻车第一名。
            </li>
            <li>
              某段标了「<b>要生成 N 次</b>」→ 它超过了单次上限:第一次用首帧出满上限,
              之后每次拿上一次的<b>尾帧</b>当首帧接着出;或者回分镜表把那一格拆成两格更可控。
            </li>
            <li>
              <b>文生视频</b>(Sora / Veo / 可灵文生)只在<b>空镜/氛围格</b>用,或者你还没出静帧
              只想先看个大概:它没有首帧,同一个角色<b>每段都可能换脸</b>。
            </li>
            <li>
              动得太夸张 → 在那一格的「怎么动」里把幅度改小(动态漫的「动」是微动:呼吸、
              衣料、发丝、眼神、光影),改完点「保存」,段计划会跟着重算。
            </li>
            <li>
              这一段出完了 → 回那一格<b>勾上「视频出好了」</b>,「成片在哪」填上文件名。
              一集几十格分几天做,下次进来直接看进度条(静帧 N/M、视频 N/M)接着做,
              不用凭记忆猜出到哪儿了。
            </li>
          </ul>
        </li>
        <li>
          <b>配音</b>:按成片包的配音稿逐条合成——剪映「文本朗读」最省事,火山引擎/MiniMax
          音色更多;每个角色挑哪种音色,看角色卡的「TTS 选型建议」与「朗读指示」。
          <br />
          带原生音频的模型(Veo 一类)自己会编对白,别用它那条人声:它不保证一字不差念你的
          台词,分段拼接处音色语速还会跳。<b>它给的环境音/动作音留着用</b>——只有人声与 BGM
          是整片后期统一铺,视频提示词里那句「不要人声」是分轨,不是让你静音。
        </li>
        <li>
          <b>拼装</b>:剪映里按剪辑清单的顺序摆片段 → 导入 SRT 压字幕 → 按 <code>bgm_tag</code>
          分段铺音乐 → 转场照清单给(冲突用快切,情绪落点用叠化)。
        </li>
        <li>
          <b>成本参考</b>:同类开源项目(Toonflow)自报一集约 ¥130——视频生成 ¥120 是大头,
          文本 ¥10,出图 &lt;¥1。按你选的平台、时长、重试次数浮动,<b>以平台实际计费为准</b>。
          想省钱就先只做静帧 + 轻微缩放平移,别每格都跑图生视频;段计划里的「要生成 N 次」
          就是你的实际计费次数,一眼看得出这一集要跑多少段。
        </li>
      </ol>
      <div className="hint drama-compliance">
        合规提醒:配音请用平台<b>正版音色库</b>,不要克隆真人声音;商用发布前确认所选音色与
        出图模型的<b>商用授权</b>。
      </div>
    </Collapsible>
  );
}
