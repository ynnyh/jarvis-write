# app/prompts/setting_cascade.py
# -*- coding: utf-8 -*-
"""设定级级联的三个 prompt:章级粗筛 → 段级定位 → 段落定点修。

与章级级联(cascade,大纲改动的下游影响)的区别:这里的作用对象是
「世界观硬规则/角色设定」这类全书有效的设定,正文任何一章都可能踩到;
粒度也从「章」细化到「段」——目标是定点修,不是整章重写。
"""


def _changes_block(changes: list[dict]) -> str:
    """把变更清单渲染成 prompt 片段(kind: changed/added/removed)。"""
    lines: list[str] = []
    for i, c in enumerate(changes, 1):
        kind = c.get("kind") or "changed"
        if kind == "removed":
            lines.append(f"{i}. 【删除】原规则:「{c.get('old', '')}」")
        elif kind == "added":
            lines.append(f"{i}. 【新增】新规则:「{c.get('new', '')}」")
        else:
            lines.append(
                f"{i}. 【修改】原:「{c.get('old', '')}」→ 新:「{c.get('new', '')}」"
            )
    return "\n".join(lines) or "(无)"


SETTING_SCREEN_PROMPT = """\
你是小说连载的"设定一致性审校"。作者刚修改了若干条【世界观硬规则/设定】,
请判断下面这一章的剧情是否受这些变更影响(受影响=按新设定该章内容需要修改)。

【设定变更】
{changes_block}

【第{chapter_number}章《{title}》内容概要】
{summary}

判断要点:
- 只在「该章的情节、描写或对话与新设定**直接冲突**」时才判受影响;
  例如规则从"主角不会剑术"改为"主角精通剑术",写过主角练剑失利/被讥讽
  不会剑的章就受影响;只提到主角名字但与新设定无关的章不受影响。
- 删除规则也可能是变更:原来受它约束的描写现在自由了,若新旧行为差异
  不影响读者理解,判不受影响。
- 模棱两可、牵强附会的一律判不受影响(宁可漏报,交给作者自己发现)。

严格按 JSON 输出(不要 markdown 代码块,不要解释):
{{
  "affected": true 或 false,
  "reason": "受影响时:一句话说明该章哪里与新设定冲突;不受影响时:留空"
}}
"""

SETTING_LOCATE_PROMPT = """\
你是小说连载的"设定一致性审校"。作者修改了若干条【世界观硬规则/设定】,
下面这一章已确认受影响。请找出**正文中与新设定冲突的具体段落**。

【设定变更】
{changes_block}

【第{chapter_number}章《{title}》正文(段落以 [P编号] 标记)】
{numbered_text}

要求:
- 只报与新设定**直接冲突**的段落;过渡句、无实质内容的段落不报。
- 每处给一个 quote:从该段落**逐字**抄出的原句(10~60 字,包含冲突点),
  系统会用它反查段落编号,必须与正文逐字一致,不许改写、不许凭印象。
- 同一处冲突只报一次;一段里有多处独立冲突可拆成多条。

严格按 JSON 输出(不要 markdown 代码块,不要解释):
{{
  "hits": [
    {{
      "quote": "从正文逐字抄出的原句",
      "reason": "这句话与新设定怎么冲突、应改成什么样"
    }}
  ]
}}
"""

SETTING_PATCH_PROMPT = """\
你是小说连载的"设定修订师"。作者修改了设定,下面这段正文与新设定冲突,
请产出**改写后的段落**,使其符合新设定。

【设定变更】
{changes_block}

【本章内容概要(供衔接参考)】
{summary}

【需要修订的段落】
{paragraph}

改写铁律(与润色相反,这里允许改事实):
- **最小改动**:只改与新设定直接冲突的事实细节(称谓、能力、规则细节、
  时间地点等),其余文字——包括语气、节奏、修辞、上下文衔接——逐字保留。
- 不要重写整段、不要扩写、不要"顺手"优化文笔;改完的字数应与原文相当。
- 若段落里出现新旧设定都要用到的同一细节,按新设定改,保持段内逻辑自洽。
- 改写后的段落必须是完整可独立成段的正文,不要输出解释。

严格按 JSON 输出(不要 markdown 代码块,不要解释):
{{
  "new_paragraph": "改写后的完整段落",
  "notes": "一句话说明改了什么(给作者看的验收提示)"
}}
"""
