# app/prompts/birthday.py
# -*- coding: utf-8 -*-
"""生日祝福提示词:30/60 秒寿星定制祝福片,一次产三个不同切入的本子。

与情绪短片共用两段式骨架(①风格卡+三条切入小输出,②三发并行展开防截断),
差异全在「定制」二字:
- 背景块是**寿星资料**(称呼/关系/里程碑/回忆点/送出人),不是抽象命题;
- 结构铁律是**三幕节奏契约**(开场点名抛悬念 → 回忆杀具体到物 → 高潮收在动作帧),
  借鉴开源 hyperframes 视频工作流的「模板+节奏契约」思路——生日片的情绪路径
  是有公式的,让模型自由发挥只会拍成蛋糕流水账;
- 回忆点红线:回忆段必须落在用户给的回忆点上,不许编造寿星没有的往事
  (引擎还会做关键词核对,对不上的进 cautions——定制片张冠李戴比平庸更糟)。

**铁律必须出现在提示词正文里**(与 clips 同一教训:写在本文件注释里模型看不见)。
"""
from __future__ import annotations

# 两段共用的结构铁律:三幕节奏契约。生日片的完播逻辑与情绪短片不同——
# 观众是被点名的人,前 3 秒「这是拍我的?」的悬念感就是钩子。
BIRTHDAY_STRUCTURE_RULES = """\
**结构铁律(三幕节奏契约)**:
- 开场 3 秒内点名或抛悬念:寿星称呼/年龄梗/只有TA才懂的那件事直接开场,
  不许蛋糕蜡烛空镜开场、不许先铺环境;
- 中段回忆杀必须**具体到物**:落在给定回忆点上的旧物/场景/动作/口头禅,
  同一段情绪不许连拍两格;
- 高潮收在动作帧:吹蜡烛/拥抱/举杯/全员喊出称呼的那一帧定格,留白,不解释;
- 祝福台词里必须自然出现寿星称呼,不许只说「生日快乐」六个字。"""

# 俗套黑名单:生日片是被拍烂最多的品类,这些桥段出现即平庸
BIRTHDAY_CLICHE_BLACKLIST = """\
**俗套黑名单**(除非翻出新意,否则禁用):「时光荏苒/岁月如梭」式空镜堆砌、
蛋糕蜡烛从头亮到尾的流水账、空洞排比祝福语(「愿你…愿你…愿你…」)、
慢镜头+煽情BGM堆砌、伪一镜到底炫技、变老特效滥用。"""

# 每格 prompt 的最低标准:六项细节标准与三线同口径(视频模型很笨,缺项就乱来),
# 外加生日特有两条:真实照片锚与称呼落词。
BIRTHDAY_PROMPT_DETAILS = """\
**写 prompt 的最低标准(缺任何一项都算没写全,宁长勿漏;不达标必改)**:
每格 `prompt_cn` 必须逐项写清:
1. 人物:出场即写全——性别/年龄段/身高体态/发型发色/脸型五官/服装(款式+颜色+材质)/
   当格动作/表情情绪。多人同框各自写清,用「A…,B…」分清。同一角色跨格必须一致
   (同一身衣服、同一发型),这是给参考图兜底的前提。**寿星本人的外貌描述
   必须与 character_desc 定妆描述逐字一致**。
2. 场景:具体空间 + 关键陈设 + 位置关系(写「厨房岛台左侧,蛋糕摆在正中」,
   不只写「家里」)。
3. 环境光线:时段/主光源方向/色温冷暖/明暗对比(如「傍晚暖黄吊灯,烛光在脸侧跳动」)。
4. 氛围:这一格的情绪基调,一句话带镜头感。
5. 景别·运镜·构图:写实际发生的运镜(推到哪、焦点在哪)与主体在画面的位置/留白方向。
6. 画面里不许出现文字/字幕/标志字体(祝福字卡是后期压的,画面里不画)。
`prompt_en` 同等覆盖人物/场景/光线/构图,只是换成英文并列短语。
**生日特有**:
7. 会拿寿星真实照片当参考图生成的格子,提示词开头写明「人物保持参考照片的
   面部特征与体型,仅还原场景与动作」——不写这句,图生视频会把脸跑漂;
8. 承载祝福/称呼的台词落进该格 dialogue,与 lines 对齐。"""

# =============== 背景块:寿星资料(两段共用) ===============
# {style_directive} 两态:无风格包=【画风方向】(media.directions 的通用画种);
# 有风格包=【风格包】(佩奇式/奥特曼式等角色世界包:强画风锚+世界观场景词+
# 主角植入指令三合一)。风格包自带画风,通用方向不再注入,避免两套硬约束打架。
BIRTHDAY_CONTEXT = """\
这是一支**寿星定制生日祝福视频**:拍的是下面这个具体的人,给TA本人(或当着
全场亲友的面)看。定制感的全部来源是资料,逐项落实、不许泛化。

【基调(硬约束)】{tone_label}
【寿星称呼】{honoree_name}
【与送出人的关系】{relationship_label}
{milestone_block}【送出人】{sender_desc}
【回忆点(回忆段只可从这里选材展开,不得编造寿星没有的往事)】
{memories_block}
【时长】{duration_s} 秒(竖屏 9:16)
{style_directive}
{hints_block}"""

# 回忆点红线(两段都带;memories 为空时引擎不注入本块)
BIRTHDAY_GROUNDING_RULE = """\
**回忆点红线**:回忆段必须落在给定回忆点上(可以展开细节,不许换事);
回忆点里没有的往事一律不许编——引擎会做关键词核对,对不上的回忆会被标进
cautions 提示用户核实。没给回忆点时,用「关系+里程碑」能推出的泛化场景,
且不许虚构具体到「某年某地某事」的细节。
"""

# =============== ① 定风格卡 + 三条切入 ===============
BIRTHDAY_TAKES_PROMPT = """\
你是顶级生日祝福片导演,给明星好友拍过刷屏的定制生日片,懂「拍一个人」和
「拍一个生日」的区别——前者才有转发,后者只是蛋糕。
{context_block}
先只做两件事:**定一套三条本子共用的画风**,再给 **3 个真正不同切入的创意方向**。
这一步不要写分镜、不要写提示词(下一步再展开)。
{structure_rules}
{cliche_blacklist}{feedback_block}
严格按 JSON 输出(不要 markdown 围栏,不要任何解释):
{{
  "style_name": "风格名一句话",
  "style_cn": "画风锁定段 60-100 字(媒介/色彩/光影/质感,不绑定具体内容)",
  "style_en": "英文关键词串 15-25 词",
  "negative": "负面提示词(通用规避 + 本风格特有)",
  "takes": [
    {{
      "take": "切入一句话(如「假装忘记的生日」,10 字内)",
      "logline": "这个本子讲什么,一句话",
      "emotion_curve": "从什么情绪起步、在哪一幕到顶、结尾落在哪(如「憋笑→回忆杀→烛光里破防」,40 字内)",
      "punchline": "结尾祝福金句字幕卡(≤16 字,要有TA的名字或专属梗,不说教)",
      "hook_text": "开头 3 秒的钩子文案(≤16 字,必须对应第一格画面)"
    }}
  ]
}}

要求:
1. 必须给满 3 条切入,且切入真正不同(不同叙事诡计/不同视角/不同情绪路径),
   不是同一个故事的三种说法;三条都必须是「给这个人的」,不是通用生日模板。
2. punchline 是灵魂:口语、专属、有画面。称呼或回忆梗要在金句里;
   拒绝「岁岁常欢愉」式套话;三条各不相同。
3. style_cn/style_en/negative 必须落在画风方向内,且不绑定任何一条切入的具体内容
   ——三条本子都要靠它统一画风。
4. hook_text 是完播率的生死线:「这是拍我的?」的悬念感要能一句话讲清,
   三条的钩子各走不同的路(整蛊伪装/物件悬念/年龄梗暴击)。
{grounding_rule}"""

# =============== ② 单条切入展开成分镜(三发并行) ===============
BIRTHDAY_EXPAND_PROMPT = """\
你是顶级生日祝福片导演兼分镜师。
{context_block}
{structure_rules}
{cliche_blacklist}
{prompt_details}
【已定的画风(逐字用,不得另立一套)】
- 中文画风锚:{style_cn}
- 英文画风锚:{style_en}
- 负面词基座:{negative}

【这一条要拍的切入(不许换故事)】
- 切入:{take}
- 一句话:{logline}
- 情绪曲线:{emotion_curve}
- 结尾祝福金句(高潮格停在它的画面上,画面里不写字):{punchline}
{feedback_block}
把它拍成完整分镜。严格按 JSON 输出(不要 markdown 围栏,不要任何解释):
{{
  "lines": [
    {{"speaker": "旁白或角色名(寿星称呼可用作角色名)", "text": "台词/旁白,单句 ≤ 20 字", "action": "这句对应的画面,25 字内"}}
  ],
  "shots": [
    {{"seq": 1, "scene_name": "场景", "characters": ["出场角色名"],
      "character_desc": "**多角色必须分段、每段以「角色名:」开头,禁止把多个角色的外貌混写进同一段**。每段写全:性别/年龄段/身高体态体型/发型发色/肤色/脸型五官(眼形眉形唇形)/着装款式+颜色+材质/配饰/标志性记忆点(痣、疤、酒窝等)/气质神态——要能独立拿去文生图出定妆参考图;寿星本人的描述全片逐字一致",
      "action_desc": "这一格画面,40 字内,必须可画",
      "shot_type": "远景/全景/中景/近景/特写", "camera": "固定/推/拉/摇/跟随/环绕",
      "dialogue": "该镜头承载的台词(与 lines 对齐,可空)", "duration_s": 3,
      "prompt_cn": "静帧中文提示词 160-280 字,含中文画风锚(逐字)+ 满足最低标准六项(人物/场景/光线/氛围/运镜/构图写全;竖版9:16)",
      "prompt_en": "英文提示词 60-90 词,含英文画风锚,主体动作光线构图齐全,结尾 --ar 9:16",
      "negative": "负面基座并入 + 本格特有"}}
  ]
}}

要求:
1. 节奏:{duration_s} 秒 = {shot_hint};开场 3 秒就是钩子(点名/悬念/年龄梗),
   高潮格停在最戳的动作帧上(留白,不加字)。
2. 台词总字数按 4 字/秒贴着时长;祝福的话宁具体勿排比;每条 line 落进某个镜头的 dialogue。
3. 回忆段只从给定回忆点选材,展开成画面;每条回忆点最多占一格,不重复。
4. 画风锚在 prompt_cn/prompt_en 里逐字保留;必须满足「写 prompt 的最低标准」六项
   + 生日特有两条;空词(温馨/感人)禁用。
5. 人物最多 3 人(寿星+送出人+点缀);寿星在各格的外貌与服装必须一致(同一身衣服),
   且引用同一套 character_desc 定妆描述。
6. 每一格的景别都要有变化(连续两格不许同景别+同运镜);近景/特写留给情绪顶点
   与「寿星认出回忆」的反应。
{grounding_rule}"""


def takes_feedback_block(prev_takes: list[dict], feedback: str) -> str:
    """换一批时的反馈块:上一批切入摘要 + 用户意见(空反馈返回空串)。"""
    if not feedback.strip():
        return ""
    lines = [
        "\n【上一批的切入(用户不满意,这批避开它们的方向,除非用户明确要求保留)】"
    ]
    for i, t in enumerate(prev_takes or [], 1):
        take = str(t.get("take") or "").strip()
        logline = str(t.get("logline") or "").strip()
        if take or logline:
            lines.append(f"{i}. {take}——{logline}")
    lines.append(f"【用户意见(最高优先级,逐条落实)】{feedback.strip()}")
    lines.append("这批三条切入必须与上一批真正不同,并落实用户意见。")
    return "\n".join(lines)


def expand_feedback_block(feedback: str) -> str:
    """单条重拍时的反馈块:只重展开、切入与画风不变,意见必须落实。"""
    if not feedback.strip():
        return ""
    return f"\n【用户对本条的意见(重拍时必须落实,切入与画风不许变)】{feedback.strip()}"
