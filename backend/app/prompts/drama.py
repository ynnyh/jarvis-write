# app/prompts/drama.py
# -*- coding: utf-8 -*-
"""漫剧工坊提示词:小说 → 漫剧拍摄手册(风格卡/角色卡/场景卡/集规划/剧本/分镜/三轨提示词)。

沿用「只产提示词」哲学(见 prompts/media.py):我们不做图像/视频/TTS,
产出的是用户拿去即梦/可灵/Midjourney/剪映直接可用的结构化手册。

一致性策略(全片统一的关键):
- 画风:STYLE_PROMPT 产「画风锁定段」,逐字注入每条分镜提示词;
- 人物:CHARACTER_PROMPT 产「锁定外貌段」,按出场角色注入对应分镜;
- 场景:SCENE_PROMPT 产「场景定调段」,同名场景跨集复用。
锚段注入由 SHOT_PROMPT_PROMPT 要求「逐字保留」+ 引擎兜底拼接双保险
(见 engines/drama/prompt_render.py)。
"""
from __future__ import annotations

# =============== 美术风格卡 ===============
STYLE_PROMPT = """\
你是漫剧(竖屏动态漫画短剧)的美术指导,要为一部小说改编漫剧定全片统一的视觉风格。
风格一旦确定,后续每一格分镜都会逐字引用你写的「画风锁定段」,所以它必须:
足够具体(说得清媒介/笔触/色彩/光影/质感),又足够普适(不绑定某一格的具体内容)。

【书名】{title}
【类型】{genre}
【一句话主题】{topic}
{concept_block}{style_memo_block}
严格按 JSON 输出(不要 markdown 围栏,不要任何解释),结构如下:
{{
  "style_name": "风格名(一句话概括,如:水墨武侠·电影感 / 赛博霓虹·厚涂)",
  "style_cn": "中文画风锁定段,60-120 字:媒介与笔触(如'国风厚涂,笔触沉稳')、色彩体系(主色/辅色)、光影基调(如'侧逆光为主,暗部占七成')、质感与品质词(如'绢本颗粒感,超精细')。写成一段连贯的话,不罗列名词。",
  "style_en": "英文关键词串,15-30 个词,逗号分隔,与 style_cn 同一风格",
  "negative": "负面提示词:漫剧通用的规避项(文字水印、五官错位、多余肢体、低分辨率、模糊)再叠加本风格的特有项"
}}

要求:
1. 风格要贴本书类型与基调(武侠不等于赛博),但不要出现具体角色与剧情。
2. style_cn 里每种维度都要落到「看得见」的说法,不写"很有氛围感"这类空词。
3. 漫剧是竖屏 9:16 连续短剧,风格要考虑连续上百格画面的可复制性,避免过于苛刻的细节要求。
"""

# =============== 角色视觉卡(批量) ===============
CHARACTER_PROMPT = """\
你是漫剧角色设计师。下面是这部小说的主要角色档案(来自故事圣经),
请为每个角色产一张「视觉卡」:锁定外貌段之后会逐字注入该角色出场的每一格分镜,
是全片人物一致性的锚,必须写得具体、可画、且角色之间不撞型。

【书名】{title}
【类型】{genre}
{characters_block}
严格按 JSON 输出(不要 markdown 围栏,不要任何解释),结构如下:
{{
  "cards": [
    {{
      "name": "角色名(与档案一致)",
      "appearance_cn": "锁定外貌段,80-150 字:年龄段、脸型五官特征(如'剑眉入鬓,眼尾细长')、发型与发色、体型身量、常穿服饰的材质与颜色(如'玄色暗纹劲装,银线滚边')。写看得见的东西,不写性格。",
      "appearance_en": "英文关键词串,15-25 个词,与 appearance_cn 同一个人",
      "outfit_cn": "标志性服饰一句话(比 appearance_cn 里的更聚焦'认出他靠这件'),30 字内",
      "voice_desc": "配音声线:性别+年龄段+音色+语气(如'青年男声,低沉克制,语速偏慢'),20 字内"
    }}
  ]
}}

要求:
1. 档案里有明确外貌描述的,忠实提取;没有的,按角色身份/年代合理补全,不与已有设定冲突。
2. 防撞型:给每个角色至少一个「一眼可辨」的视觉记忆点(发色/服饰色/标志物),主角之间尤其要拉开。
3. appearance_cn 只写外貌不写心理;服饰如档案有换装线索,取最常穿的那套。
"""

# =============== 场景定调卡(批量) ===============
SCENE_PROMPT = """\
你是漫剧场景设计师。下面是这部小说反复出现的主要场景(来自章节蓝图),
请为每个场景产一段「定调描述」:同名场景在后续所有分镜里复用这段描述,
保证跨集画面基调统一。

【书名】{title}
【类型】{genre}
{scenes_block}
严格按 JSON 输出(不要 markdown 围栏,不要任何解释),结构如下:
{{
  "scenes": [
    {{
      "name": "场景名(与清单一致)",
      "appearance_cn": "定调描述,60-120 字:空间结构、关键陈设、时代材质、光线基调、氛围。写看得见的东西。",
      "appearance_en": "英文关键词串,10-20 个词"
    }}
  ]
}}

要求:
1. 场景要能支撑多种镜头(远全景到特写)在同一定调下成立。
2. 依据小说类型给时代感(古装/民国/现代/未来),不凭空加标志性建筑以外的奇观。
"""

# =============== 集数规划 ===============
EPISODE_PLAN_PROMPT = """\
你是爆款竖屏漫剧的编剧策划,擅长把小说切成让人停不下来的短剧集。
下面是这部小说连续若干章的结构化素材(蓝图+节拍,不是全文),
请把它们切分成漫剧的「集」:每集 {duration_target_s} 秒左右,竖屏,独立成章又环环相扣。

【书名】{title}
【类型】{genre}
【改编模式】{mode_desc}
{concept_block}{chapters_block}
严格按 JSON 输出(不要 markdown 围栏,不要任何解释),结构如下:
{{
  "episodes": [
    {{
      "title": "本集标题(8-15 字,短剧风,有钩子感)",
      "source_chapter": 1,
      "hook": "开场钩子:第一格画面或第一句话是什么(3 秒内抓住人),40 字内",
      "recap": "本集梗概,50 字内,说清这一集发生什么",
      "cliffhanger": "结尾卡点:最后一格停在哪,让人必须点下一集,40 字内"
    }}
  ]
}}

要求:
1. 一章可拆多集(信息密度高的章)或数章并一集(过渡章),以「每集有独立小冲突+结尾有钩子」为准,不机械等分。
2. 开场钩子优先用冲突/悬念/反常画面,不用 scenery 铺垫。
3. cliffhanger 禁止用「欲知后事如何」式套话,要停在具体的动作/台词/画面上。
4. 顺序 strictly 按原作时序,不提前剧透后文反转;source_chapter 填该集主体内容对应的章号。
5. 首集钩子即全书最强钩子(拿最抓人的画面开场,可从第一章内取材)。
"""

# =============== 单集剧本 ===============
EPISODE_SCRIPT_PROMPT = """\
你是竖屏漫剧编剧。请把下面这一集的规划写成可执行剧本:后续会按剧本拆分镜,
所以每句台词/旁白都要有对应的「可拍画面」。

【书名】{title}
【本集信息】第 {ep_index} 集《{ep_title}》| 模式:{mode_desc} | 目标时长 {duration_target_s} 秒
【开场钩子】{hook}
【本集梗概】{recap}
【结尾卡点】{cliffhanger}
{prev_block}{characters_block}
【源章节正文(第 {source_chapter} 章,已节选)】
{chapter_text}
严格按 JSON 输出(不要 markdown 围栏,不要任何解释),结构如下:
{{
  "synopsis": "本集剧本梗概,80 字内",
  "lines": [
    {{
      "speaker": "说话人:角色名或'旁白'",
      "text": "台词或旁白正文",
      "action": "说这话时画面里在发生什么(动作/表情/环境变化),30 字内,必须可画"
    }}
  ]
}}

要求:
1. 对白演绎模式:10-20 条 lines,以角色对白驱动,旁白只做必要衔接(每集至多 3 条);
   口播解说模式:以旁白讲故事为主(8-14 条),穿插 2-5 条角色对白做「名场面」。
2. 台词口语化、短句化,一句不超过 25 字,符合竖屏短剧节奏;书面描写腔一律改掉。
3. 正文里的关键情节不丢:按梗概取舍,但冲突点、反转点、结尾卡点必须落进 lines。
4. action 写「镜头里看得见的事」,不写心理活动(心理用台词外化)。
5. 结尾最后 1-2 条 lines 必须落在 cliffhanger 上,戛然而止。
"""

# =============== 分镜表 ===============
STORYBOARD_PROMPT = """\
你是漫剧分镜师。请把下面这一集的剧本拆成分镜:每一格是一张画面(之后会生成
静帧图并轻微动起来),合计时长要贴近本集目标时长。

【本集信息】第 {ep_index} 集《{ep_title}》| 目标时长 {duration_target_s} 秒
【可用场景】(分镜的 scene_name 从这里选){scene_names}
【可用角色】(分镜的 characters 从这里选){character_names}
【剧本 lines】
{lines_block}
严格按 JSON 输出(不要 markdown 围栏,不要任何解释),结构如下:
{{
  "shots": [
    {{
      "seq": 1,
      "scene_name": "场景名",
      "characters": ["角色A"],
      "action_desc": "这一格画面:谁在做什么、什么表情、关键互动,50 字内,必须可画",
      "shot_type": "景别:远景/全景/中景/近景/特写",
      "camera": "运镜:固定/推/拉/摇/跟随/环绕",
      "dialogue": "该镜头承载的台词或旁白(从 lines 对齐过来,无台词则空字符串)",
      "duration_s": 4
    }}
  ]
}}

要求:
1. 镜头数按总时长算:每格 2-8 秒,对白格跟着台词长度走,动作格 3-5 秒,空镜/氛围格 2-3 秒。
2. 第一格直接给 hook 的画面,不做片头铺垫;最后一格停在 cliffhanger。
3. 景别有节奏:连续两格以上同景别要出效果再这么排;情绪重音用特写,空间转换用远/全景。
4. 每条 line 的台词必须落在某个镜头的 dialogue 里,不丢台词;一格最多承载一条台词。
5. characters 只列画面里出现(或其存在被暗示)的角色;scene_name 必须从可用场景里选,
   剧本 action 明确提到新地点且清单没有时,可自拟简短场景名。
6. action_desc 不写心理与抽象词,全部落成看得见的动作/表情/物件变化。
"""

# =============== 声线选型卡(批量) ===============
VOICE_CAST_PROMPT = """\
你是配音导演,熟悉主流 TTS 平台的音色库(火山引擎语音合成、MiniMax、剪映「文本朗读」、
Azure TTS)。下面是这部漫剧的角色卡(含已有声线描述),请为每个角色补一张「声线选型卡」:
配音环节拿着它去任何 TTS 平台都能直接挑到接近的音色。

【书名】{title}
【类型】{genre}
{cards_block}
严格按 JSON 输出(不要 markdown 围栏,不要任何解释),结构如下:
{{
  "casts": [
    {{
      "name": "角色名(与清单一致)",
      "tts_hint": "平台选型建议:按 剪映文本朗读 / 火山引擎 / MiniMax 各给一个具体方向(如'剪映:选「沉稳男声-纪录片」类;火山:青涩青年男声;MiniMax:deep_male'),80 字内",
      "reading_notes": "演奏指示:语速(偏快/中/偏慢)、情绪基调、重音与停顿建议(如'语速偏慢,压着说,「走镖不看路」的「看人」二字咬重'),60 字内"
    }}
  ]
}}

要求:
1. tts_hint 给的是「挑音色的方向」(性别+年龄段+音色气质),不编造具体音色 ID(各平台 ID 变动频繁)。
2. reading_notes 写给配音的人/调 TTS 参数的人看,要具体到本剧的台词气质,不写通用套话。
3. 角色之间的声线要拉开区分度(听声辨人),配角允许比主角更类型化。
"""

# =============== 成片包:配音稿润色 + 转场/配乐标注 ===============
PRODUCTION_PACK_PROMPT = """\
你是漫剧后期指导。下面是一集已定稿的分镜表,请为每一格补后期标注,
并给有台词的格出一版「朗读友好」的配音文本(TTS 直接能读、人配音照着顺口)。

【本集信息】第 {ep_index} 集《{ep_title}》| 模式:{mode_desc} | 目标时长 {duration_target_s} 秒
【分镜表】
{shots_block}
严格按 JSON 输出(不要 markdown 围栏,不要任何解释),结构如下:
{{
  "shots": [
    {{
      "seq": 1,
      "transition": "与下一格的衔接:硬切/叠化/甩镜/推入/拉出/黑场(最后一格写'定格收尾'或'黑场悬念')",
      "bgm_tag": "本格配乐情绪关键词(如'低弦压场/心跳鼓点/骤停留白/暖光钢琴'),连续同情绪的格子用同一个词",
      "tts_text": "若本格有台词:朗读版文本——去掉括号注释、数字与符号转口语(『第3章』→『第三章』)、语气词保留、断句用逗号;无台词则空字符串"
    }}
  ]
}}

要求:
1. bgm_tag 按「段」给:一个情节段落内的格子用同一情绪词,转场处才换,便于按段铺音乐。
2. transition 服务叙事:冲突升级用快切,情绪落点用叠化,悬念处用推入或黑场。
3. tts_text 必须与原台词同义,只做读音优化,不许改写内容;空镜格留空。
4. seq 与分镜表一一对应,一格不漏。
"""

# =============== 分镜三轨提示词(批量,含锚段注入) ===============
SHOT_PROMPT_PROMPT = """\
你是 AI 绘图提示词工程师,为竖屏漫剧的每一格分镜写「即拿即用」的绘图提示词。
下面先给你本片的统一锚段(画风/出场角色/场景),它们必须逐字出现在对应提示词里,
这是全片画风与人物一致性的关键——不许改写、不许意译锚段内容。

【画风锚(中文,逐字包含进每条 prompt_cn)】{style_cn}
【画风锚(英文,逐字包含进每条 prompt_en)】{style_en}
【负面词基座(并入每条 negative)】{style_negative}
【出场角色锚(该镜头角色才注入,中文外貌段逐字包含)】
{character_anchors}
【场景锚(该镜头场景才注入)】
{scene_anchors}
【分镜清单】
{shots_block}
严格按 JSON 输出(不要 markdown 围栏,不要任何解释),结构如下:
{{
  "shots": [
    {{
      "seq": 1,
      "prompt_cn": "中文提示词,120-200 字,六层结构:①主体(角色锚段+本格动作神态)②场景(场景锚段)③氛围情绪④光影⑤构图(竖版9:16+景别+机位)⑥画风(画风锚段)。锚段原样嵌入,其余按分镜 action 细化。",
      "prompt_en": "英文提示词,40-60 个词,逗号分隔,同样六层,含英文锚段,结尾加 --ar 9:16",
      "negative": "负面词基座逐字并入,再按本格内容补特有规避项"
    }}
  ]
}}

要求:
1. 锚段逐字保留是硬要求;锚段之外的部分才是你的创作空间,把 action_desc 细化成画面。
2. 同一角色在不同镜头的描述必须来自同一段角色锚,不许另起炉灶换外貌。
3. 台词内容不进画面提示词(画面里不出现对白文字),字幕后期加。
4. prompt_cn 六层顺序可以微调,但一层不能少;空词("震撼""唯美")一律不许出现。
5. 无角色的空镜:跳过角色锚,其余同样办理。
"""
