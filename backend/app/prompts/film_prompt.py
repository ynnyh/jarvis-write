# app/prompts/film_prompt.py
# -*- coding: utf-8 -*-
"""整片提示词共享模板:各工坊(漫剧/情绪/灵感/故事/宣传片)共用的分段式手艺。

外部视频模型单次最多生成 15s(少数 30s),而各工坊的成片都长于单条上限——
所以整片提示词的产物是**分段文档**:按镜头边界切好的 N 段各自独立可用的提示词
(段首逐字复述画风锚保跨段一致),逐段生成后按段号拼接。工坊差异只体现在
「类型定位」「原料块」两处,由各引擎填参;切分用 engines/media/segments 的
镜头边界贪心,绝不从镜头中间断开。
"""
from __future__ import annotations

# 单段时长上限白名单:外部模型单次生成的现实上限(15s 主流,少数支持 30s)
VALID_SEGMENT_S = (15, 30)

# 分段提示词模板:占位符由引擎填;输出是纯文本(不产 JSON,模板里没有花括号字面量)
SEGMENTED_FILM_PROMPT_TEMPLATE = """\
你是短剧导演兼提示词工程师。下面是一支{workshop_label}的分镜与设定。全片 {total_s} 秒,\
已按镜头边界切成 {seg_count} 段(每段 ≤{segment_s} 秒,绝不在镜头中间断开)。\
外部视频模型单次最多生成 {segment_s} 秒,所以你产出的不是一条整片提示词,而是\
**{seg_count} 段各自独立可用的分段提示词**:每段单独贴进模型都能生成风格、人物\
一致的片段,全部生成后按段号顺序拼接成片。

【片名/主题】{title_line}
【画幅】{ratio},全片 {total_s} 秒
【类型定位(每段描述都贴着它写)】{framing}
【画风锚(每段第一句必须逐字融入,这是跨段风格一致的关键)】{style_block}
{extra_blocks}
【出场角色档案(段内涉及到的角色,其外貌/服饰逐字写进该段;没有固定人物则照底注处理)】
{characters_block}
【分段计划(每段的内容原料;时间码按时长累计)】
{segments_block}

输出要求(纯文本,不要 markdown 围栏,不要 JSON,不要任何解释):
1. 只输出 {seg_count} 个分段提示词块,从「【第1段|起始—结束秒】」开始,一段一块,按顺序输出。
2. 每块以「【第N段|起始—结束秒】」开头;第一句逐字融入画风锚,再逐镜头描述\
(景别/运镜/画面内容/光线氛围);涉及角色时逐字融入其外貌与服饰——全片形象不变、\
不漂移、不模仿任何现实演员或公众人物。
3. 台词/解说原文逐字嵌入并标注说话人,一句都不许改写。
4. 每块结尾一行「衔接:」标注与下一段的接法——默认「硬切」;仅当本段末镜头与下一段\
首镜头是同一场景的延续运镜时,写「延续:本段末帧作下段首帧(图生视频)」;\
最后一段写「全片结束」。
5. 块内不许引用其他段(如「接上一段」「如前所述」)——每段都必须自足,单独生成也成立。
6. 中文,每段 150-350 字(按段内内容伸缩)。
"""

# 分段文档的确定性文档头:人看的使用说明,不属于任何单段提示词(引擎拼在最前)
def segmented_doc_header(seg_count: int, segment_s: int) -> str:
    return (
        f"【使用说明】本片共 {seg_count} 段,每段 ≤{segment_s} 秒。逐段单独生成:"
        f"把每段提示词分别贴进视频模型(单条上限 {segment_s} 秒的模型直接用;"
        f"支持更长单段的模型可自行合并相邻两段),生成完按段号顺序拼接"
        f"(剪映/ffmpeg concat),拼接处为硬切。跨段风格一致靠每段开头的画风锚;"
        f"标注「延续」的边界,用上一段末帧当本段首帧(图生视频)更稳。\n"
        f"================\n"
    )


# ---- 类型定位(每段描述都贴着写的那句) ----
CLIP_FRAMINGS = {
    "mood": "情绪短片:以情绪氛围为主轴的竖屏短片,画面为情绪服务,节奏与留白比信息量重要",
    "play": "灵感玩法短片:围绕一个玩法/创意命题的竖屏短片,要让观众看完「想跟着试一遍」",
    "free": "故事短片:用户原创故事改编的竖屏短片,人物与情节是主轴",
}
DRAMA_FRAMING = "漫剧(竖屏动态漫画短剧)的一集:人物与剧情是主轴,对白驱动,逐格画面的连续叙事"
PROMO_FRAMING = (
    "宣传片:为{subject}量身定制的竖屏宣传片,解说词驱动,画面以高质量空镜与地标特写为主,"
    "素材点是解说与画面的硬约束,不能编造主体没有的事实"
)
