# app/prompts/polish.py
# -*- coding: utf-8 -*-
"""润色引擎 Prompt:锁情节事实 + 去 AI 味(先诊断后治疗两段式)。

两条铁律:
1. 只改文笔,不改情节事实(锁定事实清单,润色后校验)
2. 默认去 AI 腔:检测驱动(命中点贴进 prompt 定点改写)+ 通用规则常驻

输出契约(借鉴 ai-flavor-remover 的"先诊断后治疗"):
【诊断】2-3 个最突出的 AI 味问题(类别+原句)→【策略】→【润色稿】
"""

# 去 AI 味的通用规则,始终注入(无论用户是否勾选"去AI味")
_DEAI_RULES = """\
【去除 AI 腔(务必遵守)】
- 删神态套话:"眼中闪过一丝""嘴角勾起""眸光一沉""邪魅一笑""空气仿佛凝固""命运的齿轮开始转动"等,换成具体动作
- 删稳妥套话:"沉默片刻""微微一笑""轻声说道""缓缓开口""淡淡地道"等,出现就必须改写
- 删总结过渡腔:"综上所述""总而言之""总的来说""由此可见""毫无疑问"等;段尾/章尾不得总结升华、点题说教
- 删论文式连接:"首先/其次/最后""一方面/另一方面""不仅……而且",以及"此外/而且/综上"式生硬过渡
- 删 hedge 词:"通常""往往""一般来说""某种意义上"
- 少用"仿佛""宛如""好似"等比喻连接词,能直接白描就白描;比喻限量,不堆砌
- 避免"不是……而是……""与其说……不如说……"等 AI 高频造句定式;拆掉排比堆砌
- 情绪靠动作、细节、对话呈现,不要直接写"他感到无比绝望""心中五味杂陈"这类情绪标签(Show, don't tell)
- 抽象描写必须挂具体锚点:数字、具名事物、感官细节,不写"一种难以言喻的氛围"
- 句式节奏:长短句交错,不得连续 3 句等长;破折号每 300 字最多 1 处;分号能改句号就改句号
- 保留人物特有的语气和口吻,不要把所有人都改成同一种"高级"腔调
"""

# 两段式输出契约(诊断 → 策略 → 润色稿),整章与片段共用
_OUTPUT_CONTRACT = """\
【输出契约:严格按三段输出,先诊断后治疗】
【诊断】列出本文最突出的 2-3 个 AI 味问题(类别 + 原句摘录,各一行)
【策略】每个问题对应的修改策略(各一句话)
【润色稿】润色后的完整文本(此标记之后只有正文,不要任何解释)
"""

# =============== 整章/选段润色 ===============
POLISH_PROMPT = """\
你是资深文学编辑,请润色以下小说片段。流程:先诊断,后治疗。这是纯文笔加工,不是改写剧情。

【必须锁定的情节事实(逐条保留,不得增删改)】
{locked_facts}

【本次检测命中的 AI 腔(针对这些具体命中点修改,其余好句子保持原样)】
{flavor_hits}

【待润色文本】
{text}

{style_directives}
{deai_rules}
润色铁律:
1. 情节、人物行为、对话内容的实质、时间地点因果 —— 一律不得改变
2. 不得新增原文没有的事件、人物、道具、设定
3. 不得删除任何情节事实(可以删冗余修饰,但不能删"发生了什么")
4. 只调整:遣词、句式、节奏、描写密度、语气
5. 保持与原文相当的篇幅(±15%)

{output_contract}
"""

# =============== 阅读中片段润色(带用户方向,单次调用) ===============
FRAGMENT_POLISH_PROMPT = """\
你是资深文学编辑,请润色下面这段小说正文。流程:先诊断,后治疗。这是纯文笔加工,不是改写剧情。

【本章蓝图摘要(仅作上下文,润色范围仅限给定片段)】
{chapter_summary}

【用户润色方向】
{direction}

【本次检测命中的 AI 腔(针对这些具体命中点修改,其余好句子保持原样)】
{flavor_hits}

【待润色片段】
{fragment}

润色铁律:
1. 情节、事实、人物状态、对话实质、伏笔安排 —— 一律不得改变
2. 不得新增原文没有的事件、人物、道具、设定;不得删除任何情节事实
3. 只调整:遣词、句式、节奏、描写密度、语气
4. 保持与原文相当的篇幅(±15%)
5. 严格遵循用户润色方向;方向与铁律冲突时,以铁律为准

{output_contract}
"""

# =============== 润色前:抽取情节事实清单(锁定用) ===============
FACT_LOCK_PROMPT = """\
从以下小说片段中,抽取"润色时必须保留、不得改动"的情节事实清单。

【片段】
{text}

抽取要点:
- 谁做了什么(关键动作/事件)
- 谁说了什么(对话的核心信息,不是原句)
- 状态变化(受伤/获得/失去/移动/揭露)
- 关键道具、地点、时间

严格按 JSON 输出(不要 markdown 围栏,不要解释):
{{"facts": ["事实1", "事实2", ...]}}

只列"改了就算改剧情"的硬事实,不要列修辞和描写细节。最多 15 条。
"""

# =============== 润色后:校验事实是否被动过(锁情节兜底) ===============
FACT_VERIFY_PROMPT = """\
对比润色前后两个版本,检查润色是否擅自改动了情节事实。

【润色前必须保留的事实】
{locked_facts}

【润色后文本】
{polished_text}

逐条检查每个事实在润色后文本中是否仍然成立(可以换措辞,但事实本身不能变、不能丢)。

严格按 JSON 输出(不要 markdown 围栏,不要解释):
{{"violations": [{{"fact": "被改动或丢失的事实", "problem": "怎么改的/丢在哪"}}]}}

没有违规输出 {{"violations": []}}。只报告确实的事实改动,措辞变化不算违规。
"""
